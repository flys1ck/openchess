// This file is auto-generated by @hey-api/openapi-ts

import { type Client, type Options as Options2, type TDataShape, urlSearchParamsBodySerializer } from './client';
import { client } from './client.gen';
import type { DeleteApiBulkPairingByIdData, DeleteApiBulkPairingByIdErrors, DeleteApiBulkPairingByIdResponses, DeleteApiExternalEngineByIdData, DeleteApiExternalEngineByIdResponses, DeleteApiStudyByStudyIdByChapterIdData, DeleteApiStudyByStudyIdByChapterIdResponses, DeleteApiTokenData, DeleteApiTokenResponses, GetAntichessData, GetAntichessResponses, GetApiAccountData, GetApiAccountEmailData, GetApiAccountEmailResponses, GetApiAccountKidData, GetApiAccountKidResponses, GetApiAccountPlayingData, GetApiAccountPlayingResponses, GetApiAccountPreferencesData, GetApiAccountPreferencesResponses, GetApiAccountResponses, GetApiBoardGameByGameIdChatData, GetApiBoardGameByGameIdChatResponses, GetApiBoardGameStreamByGameIdData, GetApiBoardGameStreamByGameIdErrors, GetApiBoardGameStreamByGameIdResponses, GetApiBotGameByGameIdChatData, GetApiBotGameByGameIdChatResponses, GetApiBotGameStreamByGameIdData, GetApiBotGameStreamByGameIdErrors, GetApiBotGameStreamByGameIdResponses, GetApiBotOnlineData, GetApiBotOnlineResponses, GetApiBroadcastByBroadcastTournamentIdData, GetApiBroadcastByBroadcastTournamentIdPgnData, GetApiBroadcastByBroadcastTournamentIdPgnResponses, GetApiBroadcastByBroadcastTournamentIdResponses, GetApiBroadcastByBroadcastTournamentSlugByBroadcastRoundSlugByBroadcastRoundIdData, GetApiBroadcastByBroadcastTournamentSlugByBroadcastRoundSlugByBroadcastRoundIdResponses, GetApiBroadcastByByUsernameData, GetApiBroadcastByByUsernameResponses, GetApiBroadcastData, GetApiBroadcastMyRoundsData, GetApiBroadcastMyRoundsResponses, GetApiBroadcastResponses, GetApiBroadcastRoundByBroadcastRoundIdPgnData, GetApiBroadcastRoundByBroadcastRoundIdPgnResponses, GetApiBroadcastSearchData, GetApiBroadcastSearchResponses, GetApiBroadcastTopData, GetApiBroadcastTopResponses, GetApiBulkPairingByIdData, GetApiBulkPairingByIdErrors, GetApiBulkPairingByIdGamesData, GetApiBulkPairingByIdGamesResponses, GetApiBulkPairingByIdResponses, GetApiBulkPairingData, GetApiBulkPairingResponses, GetApiChallengeByChallengeIdShowData, GetApiChallengeByChallengeIdShowResponses, GetApiChallengeData, GetApiChallengeResponses, GetApiCloudEvalData, GetApiCloudEvalErrors, GetApiCloudEvalResponses, GetApiCrosstableByUser1ByUser2Data, GetApiCrosstableByUser1ByUser2Responses, GetApiExternalEngineByIdData, GetApiExternalEngineByIdResponses, GetApiExternalEngineData, GetApiExternalEngineResponses, GetApiFidePlayerByPlayerIdData, GetApiFidePlayerByPlayerIdRatingsData, GetApiFidePlayerByPlayerIdRatingsResponses, GetApiFidePlayerByPlayerIdResponses, GetApiFidePlayerData, GetApiFidePlayerResponses, GetApiGamesExportBookmarksData, GetApiGamesExportBookmarksResponses, GetApiGamesExportImportsData, GetApiGamesExportImportsResponses, GetApiGamesUserByUsernameData, GetApiGamesUserByUsernameResponses, GetApiPlayerAutocompleteData, GetApiPlayerAutocompleteResponses, GetApiPlayerData, GetApiPlayerResponses, GetApiPlayerTopByNbByPerfTypeData, GetApiPlayerTopByNbByPerfTypeResponses, GetApiPuzzleActivityData, GetApiPuzzleActivityResponses, GetApiPuzzleBatchByAngleData, GetApiPuzzleBatchByAngleResponses, GetApiPuzzleByIdData, GetApiPuzzleByIdResponses, GetApiPuzzleDailyData, GetApiPuzzleDailyResponses, GetApiPuzzleDashboardByDaysData, GetApiPuzzleDashboardByDaysResponses, GetApiPuzzleNextData, GetApiPuzzleNextResponses, GetApiPuzzleReplayByDaysByThemeData, GetApiPuzzleReplayByDaysByThemeErrors, GetApiPuzzleReplayByDaysByThemeResponses, GetApiRacerByIdData, GetApiRacerByIdErrors, GetApiRacerByIdResponses, GetApiRelFollowingData, GetApiRelFollowingResponses, GetApiSimulData, GetApiSimulResponses, GetApiStormDashboardByUsernameData, GetApiStormDashboardByUsernameResponses, GetApiStreamBroadcastRoundByBroadcastRoundIdPgnData, GetApiStreamBroadcastRoundByBroadcastRoundIdPgnResponses, GetApiStreamerLiveData, GetApiStreamerLiveResponses, GetApiStreamEventData, GetApiStreamEventResponses, GetApiStreamGameByIdData, GetApiStreamGameByIdErrors, GetApiStreamGameByIdResponses, GetApiStudyByByUsernameData, GetApiStudyByByUsernameExportPgnData, GetApiStudyByByUsernameExportPgnResponses, GetApiStudyByByUsernameResponses, GetApiStudyByStudyIdByChapterIdPgnData, GetApiStudyByStudyIdByChapterIdPgnResponses, GetApiStudyByStudyIdPgnData, GetApiStudyByStudyIdPgnResponses, GetApiSwissByIdData, GetApiSwissByIdGamesData, GetApiSwissByIdGamesResponses, GetApiSwissByIdResponses, GetApiSwissByIdResultsData, GetApiSwissByIdResultsResponses, GetApiTeamAllData, GetApiTeamAllResponses, GetApiTeamByTeamIdArenaData, GetApiTeamByTeamIdArenaResponses, GetApiTeamByTeamIdData, GetApiTeamByTeamIdRequestsData, GetApiTeamByTeamIdRequestsResponses, GetApiTeamByTeamIdResponses, GetApiTeamByTeamIdSwissData, GetApiTeamByTeamIdSwissResponses, GetApiTeamByTeamIdUsersData, GetApiTeamByTeamIdUsersResponses, GetApiTeamOfByUsernameData, GetApiTeamOfByUsernameResponses, GetApiTeamSearchData, GetApiTeamSearchResponses, GetApiTimelineData, GetApiTimelineResponses, GetApiTournamentByIdData, GetApiTournamentByIdGamesData, GetApiTournamentByIdGamesResponses, GetApiTournamentByIdResponses, GetApiTournamentByIdResultsData, GetApiTournamentByIdResultsResponses, GetApiTournamentByIdTeamsData, GetApiTournamentByIdTeamsResponses, GetApiTournamentData, GetApiTournamentResponses, GetApiTvByChannelData, GetApiTvByChannelFeedData, GetApiTvByChannelFeedResponses, GetApiTvByChannelResponses, GetApiTvChannelsData, GetApiTvChannelsResponses, GetApiTvFeedData, GetApiTvFeedResponses, GetApiUserByUsernameActivityData, GetApiUserByUsernameActivityResponses, GetApiUserByUsernameCurrentGameData, GetApiUserByUsernameCurrentGameResponses, GetApiUserByUsernameData, GetApiUserByUsernameNoteData, GetApiUserByUsernameNoteResponses, GetApiUserByUsernamePerfByPerfData, GetApiUserByUsernamePerfByPerfResponses, GetApiUserByUsernameRatingHistoryData, GetApiUserByUsernameRatingHistoryResponses, GetApiUserByUsernameResponses, GetApiUserByUsernameTournamentCreatedData, GetApiUserByUsernameTournamentCreatedResponses, GetApiUserByUsernameTournamentPlayedData, GetApiUserByUsernameTournamentPlayedResponses, GetApiUsersStatusData, GetApiUsersStatusResponses, GetAtomicData, GetAtomicResponses, GetBroadcastByBroadcastTournamentIdPlayersByPlayerIdData, GetBroadcastByBroadcastTournamentIdPlayersByPlayerIdErrors, GetBroadcastByBroadcastTournamentIdPlayersByPlayerIdResponses, GetBroadcastByBroadcastTournamentIdPlayersData, GetBroadcastByBroadcastTournamentIdPlayersResponses, GetBroadcastByBroadcastTournamentIdTeamsStandingsData, GetBroadcastByBroadcastTournamentIdTeamsStandingsErrors, GetBroadcastByBroadcastTournamentIdTeamsStandingsResponses, GetGameByGameIdChatData, GetGameByGameIdChatResponses, GetGameExportByGameIdData, GetGameExportByGameIdResponses, GetLichessData, GetLichessResponses, GetMastersData, GetMastersPgnByGameIdData, GetMastersPgnByGameIdResponses, GetMastersResponses, GetOauthData, GetOauthResponses, GetPlayerData, GetPlayerResponses, GetStandardData, GetStandardResponses, GetSwissByIdTrfData, GetSwissByIdTrfResponses, HeadApiStudyByStudyIdPgnData, HeadApiStudyByStudyIdPgnResponses, PostApiAccountKidData, PostApiAccountKidResponses, PostApiBoardGameByGameIdAbortData, PostApiBoardGameByGameIdAbortErrors, PostApiBoardGameByGameIdAbortResponses, PostApiBoardGameByGameIdBerserkData, PostApiBoardGameByGameIdBerserkErrors, PostApiBoardGameByGameIdBerserkResponses, PostApiBoardGameByGameIdChatData, PostApiBoardGameByGameIdChatErrors, PostApiBoardGameByGameIdChatResponses, PostApiBoardGameByGameIdClaimDrawData, PostApiBoardGameByGameIdClaimDrawErrors, PostApiBoardGameByGameIdClaimDrawResponses, PostApiBoardGameByGameIdClaimVictoryData, PostApiBoardGameByGameIdClaimVictoryErrors, PostApiBoardGameByGameIdClaimVictoryResponses, PostApiBoardGameByGameIdDrawByAcceptData, PostApiBoardGameByGameIdDrawByAcceptErrors, PostApiBoardGameByGameIdDrawByAcceptResponses, PostApiBoardGameByGameIdMoveByMoveData, PostApiBoardGameByGameIdMoveByMoveErrors, PostApiBoardGameByGameIdMoveByMoveResponses, PostApiBoardGameByGameIdResignData, PostApiBoardGameByGameIdResignErrors, PostApiBoardGameByGameIdResignResponses, PostApiBoardGameByGameIdTakebackByAcceptData, PostApiBoardGameByGameIdTakebackByAcceptErrors, PostApiBoardGameByGameIdTakebackByAcceptResponses, PostApiBoardSeekData, PostApiBoardSeekErrors, PostApiBoardSeekResponses, PostApiBotAccountUpgradeData, PostApiBotAccountUpgradeErrors, PostApiBotAccountUpgradeResponses, PostApiBotGameByGameIdAbortData, PostApiBotGameByGameIdAbortErrors, PostApiBotGameByGameIdAbortResponses, PostApiBotGameByGameIdChatData, PostApiBotGameByGameIdChatErrors, PostApiBotGameByGameIdChatResponses, PostApiBotGameByGameIdClaimDrawData, PostApiBotGameByGameIdClaimDrawErrors, PostApiBotGameByGameIdClaimDrawResponses, PostApiBotGameByGameIdClaimVictoryData, PostApiBotGameByGameIdClaimVictoryErrors, PostApiBotGameByGameIdClaimVictoryResponses, PostApiBotGameByGameIdDrawByAcceptData, PostApiBotGameByGameIdDrawByAcceptErrors, PostApiBotGameByGameIdDrawByAcceptResponses, PostApiBotGameByGameIdMoveByMoveData, PostApiBotGameByGameIdMoveByMoveErrors, PostApiBotGameByGameIdMoveByMoveResponses, PostApiBotGameByGameIdResignData, PostApiBotGameByGameIdResignErrors, PostApiBotGameByGameIdResignResponses, PostApiBotGameByGameIdTakebackByAcceptData, PostApiBotGameByGameIdTakebackByAcceptErrors, PostApiBotGameByGameIdTakebackByAcceptResponses, PostApiBroadcastRoundByBroadcastRoundIdPushData, PostApiBroadcastRoundByBroadcastRoundIdPushErrors, PostApiBroadcastRoundByBroadcastRoundIdPushResponses, PostApiBroadcastRoundByBroadcastRoundIdResetData, PostApiBroadcastRoundByBroadcastRoundIdResetResponses, PostApiBulkPairingByIdStartClocksData, PostApiBulkPairingByIdStartClocksErrors, PostApiBulkPairingByIdStartClocksResponses, PostApiBulkPairingData, PostApiBulkPairingErrors, PostApiBulkPairingResponses, PostApiChallengeAiData, PostApiChallengeAiErrors, PostApiChallengeAiResponses, PostApiChallengeByChallengeIdAcceptData, PostApiChallengeByChallengeIdAcceptErrors, PostApiChallengeByChallengeIdAcceptResponses, PostApiChallengeByChallengeIdCancelData, PostApiChallengeByChallengeIdCancelErrors, PostApiChallengeByChallengeIdCancelResponses, PostApiChallengeByChallengeIdDeclineData, PostApiChallengeByChallengeIdDeclineErrors, PostApiChallengeByChallengeIdDeclineResponses, PostApiChallengeByGameIdStartClocksData, PostApiChallengeByGameIdStartClocksResponses, PostApiChallengeByUsernameData, PostApiChallengeByUsernameErrors, PostApiChallengeByUsernameResponses, PostApiChallengeOpenData, PostApiChallengeOpenErrors, PostApiChallengeOpenResponses, PostApiExternalEngineByIdAnalyseData, PostApiExternalEngineByIdAnalyseResponses, PostApiExternalEngineData, PostApiExternalEngineResponses, PostApiExternalEngineWorkByIdData, PostApiExternalEngineWorkByIdResponses, PostApiExternalEngineWorkData, PostApiExternalEngineWorkResponses, PostApiGamesExportIdsData, PostApiGamesExportIdsResponses, PostApiImportData, PostApiImportResponses, PostApiPuzzleBatchByAngleData, PostApiPuzzleBatchByAngleResponses, PostApiRacerData, PostApiRacerResponses, PostApiRelBlockByUsernameData, PostApiRelBlockByUsernameResponses, PostApiRelFollowByUsernameData, PostApiRelFollowByUsernameResponses, PostApiRelUnblockByUsernameData, PostApiRelUnblockByUsernameResponses, PostApiRelUnfollowByUsernameData, PostApiRelUnfollowByUsernameResponses, PostApiRoundByGameIdAddTimeBySecondsData, PostApiRoundByGameIdAddTimeBySecondsResponses, PostApiStreamGamesByStreamIdAddData, PostApiStreamGamesByStreamIdAddResponses, PostApiStreamGamesByStreamIdData, PostApiStreamGamesByStreamIdResponses, PostApiStreamGamesByUsersData, PostApiStreamGamesByUsersResponses, PostApiStudyByStudyIdByChapterIdMovesData, PostApiStudyByStudyIdByChapterIdMovesErrors, PostApiStudyByStudyIdByChapterIdMovesResponses, PostApiStudyByStudyIdByChapterIdTagsData, PostApiStudyByStudyIdByChapterIdTagsErrors, PostApiStudyByStudyIdByChapterIdTagsResponses, PostApiStudyByStudyIdImportPgnData, PostApiStudyByStudyIdImportPgnErrors, PostApiStudyByStudyIdImportPgnResponses, PostApiStudyData, PostApiStudyErrors, PostApiStudyResponses, PostApiSwissByIdEditData, PostApiSwissByIdEditErrors, PostApiSwissByIdEditResponses, PostApiSwissByIdJoinData, PostApiSwissByIdJoinErrors, PostApiSwissByIdJoinResponses, PostApiSwissByIdScheduleNextRoundData, PostApiSwissByIdScheduleNextRoundErrors, PostApiSwissByIdScheduleNextRoundResponses, PostApiSwissByIdTerminateData, PostApiSwissByIdTerminateErrors, PostApiSwissByIdTerminateResponses, PostApiSwissByIdWithdrawData, PostApiSwissByIdWithdrawResponses, PostApiSwissNewByTeamIdData, PostApiSwissNewByTeamIdErrors, PostApiSwissNewByTeamIdResponses, PostApiTeamByTeamIdKickByUserIdData, PostApiTeamByTeamIdKickByUserIdResponses, PostApiTeamByTeamIdRequestByUserIdAcceptData, PostApiTeamByTeamIdRequestByUserIdAcceptResponses, PostApiTeamByTeamIdRequestByUserIdDeclineData, PostApiTeamByTeamIdRequestByUserIdDeclineResponses, PostApiTokenAdminChallengeData, PostApiTokenAdminChallengeErrors, PostApiTokenAdminChallengeResponses, PostApiTokenData, PostApiTokenErrors, PostApiTokenResponses, PostApiTokenTestData, PostApiTokenTestResponses, PostApiTournamentByIdData, PostApiTournamentByIdErrors, PostApiTournamentByIdJoinData, PostApiTournamentByIdJoinErrors, PostApiTournamentByIdJoinResponses, PostApiTournamentByIdResponses, PostApiTournamentByIdTerminateData, PostApiTournamentByIdTerminateErrors, PostApiTournamentByIdTerminateResponses, PostApiTournamentByIdWithdrawData, PostApiTournamentByIdWithdrawErrors, PostApiTournamentByIdWithdrawResponses, PostApiTournamentData, PostApiTournamentErrors, PostApiTournamentResponses, PostApiTournamentTeamBattleByIdData, PostApiTournamentTeamBattleByIdErrors, PostApiTournamentTeamBattleByIdResponses, PostApiUserByUsernameNoteData, PostApiUserByUsernameNoteResponses, PostApiUsersData, PostApiUsersResponses, PostBroadcastByBroadcastTournamentIdEditData, PostBroadcastByBroadcastTournamentIdEditErrors, PostBroadcastByBroadcastTournamentIdEditResponses, PostBroadcastByBroadcastTournamentIdNewData, PostBroadcastByBroadcastTournamentIdNewErrors, PostBroadcastByBroadcastTournamentIdNewResponses, PostBroadcastNewData, PostBroadcastNewErrors, PostBroadcastNewResponses, PostBroadcastRoundByBroadcastRoundIdEditData, PostBroadcastRoundByBroadcastRoundIdEditErrors, PostBroadcastRoundByBroadcastRoundIdEditResponses, PostInboxByUsernameData, PostInboxByUsernameErrors, PostInboxByUsernameResponses, PostTeamByTeamIdJoinData, PostTeamByTeamIdJoinResponses, PostTeamByTeamIdPmAllData, PostTeamByTeamIdPmAllErrors, PostTeamByTeamIdPmAllResponses, PostTeamByTeamIdQuitData, PostTeamByTeamIdQuitResponses, PutApiExternalEngineByIdData, PutApiExternalEngineByIdResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Get real-time users status
 *
 * Read the `online`, `playing` and `streaming` flags of several users.
 * This API is very fast and cheap on lichess side.
 * So you can call it quite often (like once every 5 seconds).
 * Use it to track players and know when they're connected on lichess and playing games.
 *
 */
export const getRealtimeUsersStatus = <ThrowOnError extends boolean = false>(options: Options<GetApiUsersStatusData, ThrowOnError>) => (options.client ?? client).get<GetApiUsersStatusResponses, unknown, ThrowOnError>({ url: '/api/users/status', ...options });

/**
 * Get all top 10
 *
 * Get the top 10 players for each speed and variant.
 * See <https://lichess.org/player>.
 *
 */
export const getAllTop10 = <ThrowOnError extends boolean = false>(options?: Options<GetApiPlayerData, ThrowOnError>) => (options?.client ?? client).get<GetApiPlayerResponses, unknown, ThrowOnError>({ url: '/api/player', ...options });

/**
 * Get one leaderboard
 *
 * Get the leaderboard for a single speed or variant (a.k.a. `perfType`).
 * There is no leaderboard for correspondence or puzzles.
 * See <https://lichess.org/player/top/200/bullet>.
 *
 */
export const getOneLeaderboard = <ThrowOnError extends boolean = false>(options: Options<GetApiPlayerTopByNbByPerfTypeData, ThrowOnError>) => (options.client ?? client).get<GetApiPlayerTopByNbByPerfTypeResponses, unknown, ThrowOnError>({ url: '/api/player/top/{nb}/{perfType}', ...options });

/**
 * Get user public data
 *
 * Read public data of a user.
 *
 */
export const getUserPublicData = <ThrowOnError extends boolean = false>(options: Options<GetApiUserByUsernameData, ThrowOnError>) => (options.client ?? client).get<GetApiUserByUsernameResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/user/{username}',
    ...options
});

/**
 * Get rating history of a user
 *
 * Read rating history of a user, for all perf types.
 * There is at most one entry per day.
 * Format of an entry is `[year, month, day, rating]`.
 * `month` starts at zero (January).
 *
 */
export const getRatingHistoryOfAuser = <ThrowOnError extends boolean = false>(options: Options<GetApiUserByUsernameRatingHistoryData, ThrowOnError>) => (options.client ?? client).get<GetApiUserByUsernameRatingHistoryResponses, unknown, ThrowOnError>({ url: '/api/user/{username}/rating-history', ...options });

/**
 * Get performance statistics of a user
 *
 * Read performance statistics of a user, for a single performance.
 * Similar to the [performance pages on the website](https://lichess.org/@/thibault/perf/bullet).
 *
 */
export const getPerformanceStatisticsOfAuser = <ThrowOnError extends boolean = false>(options: Options<GetApiUserByUsernamePerfByPerfData, ThrowOnError>) => (options.client ?? client).get<GetApiUserByUsernamePerfByPerfResponses, unknown, ThrowOnError>({ url: '/api/user/{username}/perf/{perf}', ...options });

/**
 * Get user activity
 *
 * Read data to generate the activity feed of a user.
 *
 */
export const getUserActivity = <ThrowOnError extends boolean = false>(options: Options<GetApiUserByUsernameActivityData, ThrowOnError>) => (options.client ?? client).get<GetApiUserByUsernameActivityResponses, unknown, ThrowOnError>({ url: '/api/user/{username}/activity', ...options });

/**
 * Get the daily puzzle
 *
 * Get the daily Lichess puzzle in JSON format.
 * Alternatively, you can [post it in your slack workspace](https://lichess.org/daily-puzzle-slack).
 *
 */
export const getTheDailyPuzzle = <ThrowOnError extends boolean = false>(options?: Options<GetApiPuzzleDailyData, ThrowOnError>) => (options?.client ?? client).get<GetApiPuzzleDailyResponses, unknown, ThrowOnError>({ url: '/api/puzzle/daily', ...options });

/**
 * Get a puzzle by its ID
 *
 * Get a single Lichess puzzle in JSON format.
 */
export const getAPuzzleByItsId = <ThrowOnError extends boolean = false>(options: Options<GetApiPuzzleByIdData, ThrowOnError>) => (options.client ?? client).get<GetApiPuzzleByIdResponses, unknown, ThrowOnError>({ url: '/api/puzzle/{id}', ...options });

/**
 * Get a new puzzle
 *
 * Get a random Lichess puzzle in JSON format.
 *
 * If authenticated, only returns puzzles that the user has never seen before.
 *
 * **DO NOT** use this endpoint to enumerate puzzles for mass download. Instead, download the [full public puzzle database](https://database.lichess.org/#puzzles).
 *
 */
export const getANewPuzzle = <ThrowOnError extends boolean = false>(options?: Options<GetApiPuzzleNextData, ThrowOnError>) => (options?.client ?? client).get<GetApiPuzzleNextResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/puzzle/next',
    ...options
});

/**
 * Get multiple puzzles at once
 *
 * Get a batch of random Lichess puzzles in JSON format.
 *
 * If authenticated, only returns puzzles that the user has never seen before.
 *
 * **DO NOT** use this endpoint to enumerate puzzles for mass download. Instead, download the [full public puzzle database](https://database.lichess.org/#puzzles).
 *
 */
export const getMultiplePuzzlesAtOnce = <ThrowOnError extends boolean = false>(options: Options<GetApiPuzzleBatchByAngleData, ThrowOnError>) => (options.client ?? client).get<GetApiPuzzleBatchByAngleResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/puzzle/batch/{angle}',
    ...options
});

/**
 * Solve multiple puzzles at once
 *
 * Set puzzles as solved and update ratings.
 *
 */
export const solveMultiplePuzzlesAtOnce = <ThrowOnError extends boolean = false>(options: Options<PostApiPuzzleBatchByAngleData, ThrowOnError>) => (options.client ?? client).post<PostApiPuzzleBatchByAngleResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/puzzle/batch/{angle}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get your puzzle activity
 *
 * Download your puzzle activity in [ndjson](#description/streaming-with-nd-json) format.
 * Puzzle activity is sorted by reverse chronological order (most recent first)
 * We recommend streaming the response, for it can be very long.
 *
 */
export const getYourPuzzleActivity = <ThrowOnError extends boolean = false>(options?: Options<GetApiPuzzleActivityData, ThrowOnError>) => (options?.client ?? client).get<GetApiPuzzleActivityResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/puzzle/activity',
    ...options
});

/**
 * Get puzzles to replay
 *
 * Gets the puzzle IDs of remaining puzzles to re-attempt in JSON format.
 */
export const getPuzzlesToReplay = <ThrowOnError extends boolean = false>(options: Options<GetApiPuzzleReplayByDaysByThemeData, ThrowOnError>) => (options.client ?? client).get<GetApiPuzzleReplayByDaysByThemeResponses, GetApiPuzzleReplayByDaysByThemeErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/puzzle/replay/{days}/{theme}',
    ...options
});

/**
 * Get your puzzle dashboard
 *
 * Download your [puzzle dashboard](https://lichess.org/training/dashboard/30/dashboard) as JSON.
 * Also includes all puzzle themes played, with aggregated results.
 * Allows re-creating the [improvement/strengths](https://lichess.org/training/dashboard/30/improvementAreas) interfaces.
 *
 */
export const getYourPuzzleDashboard = <ThrowOnError extends boolean = false>(options: Options<GetApiPuzzleDashboardByDaysData, ThrowOnError>) => (options.client ?? client).get<GetApiPuzzleDashboardByDaysResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/puzzle/dashboard/{days}',
    ...options
});

/**
 * Get the storm dashboard of a player
 *
 * Download the [storm dashboard](https://lichess.org/storm/dashboard/mrbasso) of any player as JSON.
 * Contains the aggregated highscores, and the history of storm runs aggregated by days.
 * Use `?days=0` if you only care about the highscores.
 *
 */
export const getTheStormDashboardOfAPlayer = <ThrowOnError extends boolean = false>(options: Options<GetApiStormDashboardByUsernameData, ThrowOnError>) => (options.client ?? client).get<GetApiStormDashboardByUsernameResponses, unknown, ThrowOnError>({ url: '/api/storm/dashboard/{username}', ...options });

/**
 * Create and join a puzzle race
 *
 * Create a new private [puzzle race](https://lichess.org/racer).
 * The Lichess user who creates the race must join the race page,
 * and manually start the race when enough players have joined.
 * - <https://lichess.org/racer>
 *
 */
export const createAndJoinAPuzzleRace = <ThrowOnError extends boolean = false>(options?: Options<PostApiRacerData, ThrowOnError>) => (options?.client ?? client).post<PostApiRacerResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/racer',
    ...options
});

/**
 * Get puzzle race results
 *
 * Get the results of a [puzzle race](https://lichess.org/racer).
 * Returns information about players, puzzles, and the current status of the race.
 * - <https://lichess.org/racer>
 *
 * Note that Lichess puzzle races are not persisted, and are only available
 * for 30 minutes. After that delay, they are permanently deleted.
 *
 */
export const getPuzzleRaceResults = <ThrowOnError extends boolean = false>(options: Options<GetApiRacerByIdData, ThrowOnError>) => (options.client ?? client).get<GetApiRacerByIdResponses, GetApiRacerByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/racer/{id}',
    ...options
});

/**
 * Get users by ID
 *
 * Get up to 300 users by their IDs. Users are returned in the same order as the IDs.
 * The method is `POST` to allow a longer list of IDs to be sent in the request body.
 * Please do not try to download all the Lichess users with this endpoint, or any other endpoint.
 * An API is not a way to fully export a website. We do not provide a full download of the Lichess users.
 * This endpoint is limited to 8,000 users every 10 minutes, and 120,000 every day.
 *
 */
export const getUsersById = <ThrowOnError extends boolean = false>(options: Options<PostApiUsersData, ThrowOnError>) => (options.client ?? client).post<PostApiUsersResponses, unknown, ThrowOnError>({
    bodySerializer: null,
    url: '/api/users',
    ...options,
    headers: {
        'Content-Type': 'text/plain',
        ...options.headers
    }
});

/**
 * Get my profile
 *
 * Public information about the logged in user.
 *
 */
export const getMyProfile = <ThrowOnError extends boolean = false>(options?: Options<GetApiAccountData, ThrowOnError>) => (options?.client ?? client).get<GetApiAccountResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/account',
    ...options
});

/**
 * Get my email address
 *
 * Read the email address of the logged in user.
 *
 */
export const getMyEmailAddress = <ThrowOnError extends boolean = false>(options?: Options<GetApiAccountEmailData, ThrowOnError>) => (options?.client ?? client).get<GetApiAccountEmailResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/account/email',
    ...options
});

/**
 * Get my preferences
 *
 * Read the preferences of the logged in user.
 * - <https://lichess.org/account/preferences/game-display>
 * - <https://github.com/ornicar/lila/blob/master/modules/pref/src/main/Pref.scala>
 *
 */
export const getMyPreferences = <ThrowOnError extends boolean = false>(options?: Options<GetApiAccountPreferencesData, ThrowOnError>) => (options?.client ?? client).get<GetApiAccountPreferencesResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/account/preferences',
    ...options
});

/**
 * Get my kid mode status
 *
 * Read the kid mode status of the logged in user.
 * - <https://lichess.org/account/kid>
 *
 */
export const getMyKidModeStatus = <ThrowOnError extends boolean = false>(options?: Options<GetApiAccountKidData, ThrowOnError>) => (options?.client ?? client).get<GetApiAccountKidResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/account/kid',
    ...options
});

/**
 * Set my kid mode status
 *
 * Set the kid mode status of the logged in user.
 * - <https://lichess.org/account/kid>
 *
 */
export const setMyKidModeStatus = <ThrowOnError extends boolean = false>(options: Options<PostApiAccountKidData, ThrowOnError>) => (options.client ?? client).post<PostApiAccountKidResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/account/kid',
    ...options
});

/**
 * Get my timeline
 *
 * Get the timeline events of the logged in user.
 *
 */
export const getMyTimeline = <ThrowOnError extends boolean = false>(options?: Options<GetApiTimelineData, ThrowOnError>) => (options?.client ?? client).get<GetApiTimelineResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/timeline',
    ...options
});

/**
 * Export one game
 *
 * Download one game in either PGN or JSON format.
 * Ongoing games are delayed by 3 moves, as to prevent cheat bots from using this API.
 *
 */
export const exportOneGame = <ThrowOnError extends boolean = false>(options: Options<GetGameExportByGameIdData, ThrowOnError>) => (options.client ?? client).get<GetGameExportByGameIdResponses, unknown, ThrowOnError>({ url: '/game/export/{gameId}', ...options });

/**
 * Fetch the spectator game chat
 *
 * Get the messages posted in the public spectator chat of a game.
 *
 * Games also have a private players chat, which only the 2 players can see.
 *
 */
export const fetchTheSpectatorGameChat = <ThrowOnError extends boolean = false>(options: Options<GetGameByGameIdChatData, ThrowOnError>) => (options.client ?? client).get<GetGameByGameIdChatResponses, unknown, ThrowOnError>({ url: '/game/{gameId}/chat', ...options });

/**
 * Export ongoing game of a user
 *
 * Download the ongoing game, or the last game played, of a user.
 * Available in either PGN or JSON format.
 * Ongoing games are delayed by 3 moves, as to prevent cheat bots from using this API.
 *
 */
export const exportOngoingGameOfAuser = <ThrowOnError extends boolean = false>(options: Options<GetApiUserByUsernameCurrentGameData, ThrowOnError>) => (options.client ?? client).get<GetApiUserByUsernameCurrentGameResponses, unknown, ThrowOnError>({ url: '/api/user/{username}/current-game', ...options });

/**
 * Export games of a user
 *
 * Download all games of any user in PGN or [ndjson](#description/streaming-with-nd-json) format.
 * Games are sorted by reverse chronological order (most recent first).
 * We recommend streaming the response, for it can be very long.
 * <https://lichess.org/@/german11> for instance has more than 500,000 games.
 * The game stream is throttled, depending on who is making the request:
 * - Anonymous request: 20 games per second
 * - [OAuth2 authenticated](#description/authentication) request: 30 games per second
 * - Authenticated, downloading your own games: 60 games per second
 *
 */
export const exportGamesOfAuser = <ThrowOnError extends boolean = false>(options: Options<GetApiGamesUserByUsernameData, ThrowOnError>) => (options.client ?? client).get<GetApiGamesUserByUsernameResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/games/user/{username}',
    ...options
});

/**
 * Export games by IDs
 *
 * Download games by IDs in PGN or [ndjson](#description/streaming-with-nd-json) format, depending on the request `Accept` header.
 * Games are sorted by reverse chronological order (most recent first)
 * The method is `POST` so a longer list of IDs can be sent in the request body.
 * 300 IDs can be submitted.
 * Ongoing games are delayed by 3 moves, as to prevent cheat bots from using this API.
 *
 */
export const exportGamesByIds = <ThrowOnError extends boolean = false>(options: Options<PostApiGamesExportIdsData, ThrowOnError>) => (options.client ?? client).post<PostApiGamesExportIdsResponses, unknown, ThrowOnError>({
    bodySerializer: null,
    url: '/api/games/export/_ids',
    ...options,
    headers: {
        'Content-Type': 'text/plain',
        ...options.headers
    }
});

/**
 * Stream games of users
 *
 * Stream the games played between a list of users, in real time.
 * Only games where **both players** are part of the list are included.
 * The stream emits an event each time a game is started or finished.
 * To also get all current ongoing games at the beginning of the stream, use the `withCurrentGames` flag.
 * Games are streamed as [ndjson](#description/streaming-with-nd-json).
 * Maximum number of users: 300.
 * The method is `POST` so a longer list of IDs can be sent in the request body.
 *
 */
export const streamGamesOfUsers = <ThrowOnError extends boolean = false>(options: Options<PostApiStreamGamesByUsersData, ThrowOnError>) => (options.client ?? client).post<PostApiStreamGamesByUsersResponses, unknown, ThrowOnError>({
    bodySerializer: null,
    url: '/api/stream/games-by-users',
    ...options,
    headers: {
        'Content-Type': 'text/plain',
        ...options.headers
    }
});

/**
 * Stream games by IDs
 *
 * Creates a stream of games from an arbitrary streamId, and a list of game IDs.
 * The stream first outputs the games that already exists, then emits an event each time a game is started or finished.
 * Games are streamed as [ndjson](#description/streaming-with-nd-json).
 * Maximum number of games: 500 for anonymous requests, or 1000 for [OAuth2 authenticated](#description/authentication) requests.
 * While the stream is open, it is possible to [add new game IDs to watch](#tag/games/POST/api/stream/games/{streamId}/add).
 *
 */
export const streamGamesByIds = <ThrowOnError extends boolean = false>(options: Options<PostApiStreamGamesByStreamIdData, ThrowOnError>) => (options.client ?? client).post<PostApiStreamGamesByStreamIdResponses, unknown, ThrowOnError>({
    bodySerializer: null,
    url: '/api/stream/games/{streamId}',
    ...options,
    headers: {
        'Content-Type': 'text/plain',
        ...options.headers
    }
});

/**
 * Add game IDs to stream
 *
 * Add new game IDs for [an existing stream](#tag/games/POST/api/stream/games/{streamId}) to watch.
 * The stream will immediately outputs the games that already exists, then emit an event each time a game is started or finished.
 *
 */
export const addGameIdsToStream = <ThrowOnError extends boolean = false>(options: Options<PostApiStreamGamesByStreamIdAddData, ThrowOnError>) => (options.client ?? client).post<PostApiStreamGamesByStreamIdAddResponses, unknown, ThrowOnError>({
    bodySerializer: null,
    url: '/api/stream/games/{streamId}/add',
    ...options,
    headers: {
        'Content-Type': 'text/plain',
        ...options.headers
    }
});

/**
 * Get my ongoing games
 *
 * Get the ongoing games of the current user.
 * Real-time and correspondence games are included.
 * The most urgent games are listed first.
 *
 */
export const getMyOngoingGames = <ThrowOnError extends boolean = false>(options?: Options<GetApiAccountPlayingData, ThrowOnError>) => (options?.client ?? client).get<GetApiAccountPlayingResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/account/playing',
    ...options
});

/**
 * Stream moves of a game
 *
 * Stream positions and moves of any ongoing game, in [ndjson](#description/streaming-with-nd-json).
 * A description of the game is sent as a first message.
 * Then a message is sent each time a move is played.
 * Finally, a description of the game is sent when it finishes, and the stream is closed.
 * Ongoing games are delayed by 3 moves, as to prevent cheat bots from using this API.
 * No more than 8 game streams can be opened at the same time from the same IP address.
 *
 */
export const streamMovesOfAGame = <ThrowOnError extends boolean = false>(options: Options<GetApiStreamGameByIdData, ThrowOnError>) => (options.client ?? client).get<GetApiStreamGameByIdResponses, GetApiStreamGameByIdErrors, ThrowOnError>({ url: '/api/stream/game/{id}', ...options });

/**
 * Import one game
 *
 * Import a game from PGN. See <https://lichess.org/paste>.
 * Rate limiting: 200 games per hour for OAuth requests, 100 games per hour for anonymous requests.
 * To broadcast ongoing games, consider [pushing to a broadcast instead](#tag/broadcasts/POST/api/broadcast/round/{broadcastRoundId}/push).
 * To analyse a position or a line, just construct an analysis board URL (most standard tags supported if URL-encoded):
 * [https://lichess.org/analysis/pgn/e4_e5_Nf3_Nc6_Bc4_Bc5_Bxf7+](https://lichess.org/analysis/pgn/e4_e5_Nf3_Nc6_Bc4_Bc5_Bxf7+)
 *
 */
export const importOneGame = <ThrowOnError extends boolean = false>(options: Options<PostApiImportData, ThrowOnError>) => (options.client ?? client).post<PostApiImportResponses, unknown, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/import',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Export your imported games
 *
 * Download all games imported by you. Games are exported in PGN format.
 */
export const exportYourImportedGames = <ThrowOnError extends boolean = false>(options?: Options<GetApiGamesExportImportsData, ThrowOnError>) => (options?.client ?? client).get<GetApiGamesExportImportsResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/games/export/imports',
    ...options
});

/**
 * Export your bookmarked games
 *
 * Download all games bookmarked by you, in PGN or [ndjson](#description/streaming-with-nd-json) format.
 * Games are sorted by reverse chronological order (most recent first).
 * We recommend streaming the response, for it can be very long.
 *
 */
export const exportYourBookmarkedGames = <ThrowOnError extends boolean = false>(options?: Options<GetApiGamesExportBookmarksData, ThrowOnError>) => (options?.client ?? client).get<GetApiGamesExportBookmarksResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/games/export/bookmarks',
    ...options
});

/**
 * Get current TV games
 *
 * Get basic info about the best games being played for each speed and variant,
 * but also computer games and bot games.
 * See [lichess.org/tv](https://lichess.org/tv).
 *
 */
export const getCurrentTvGames = <ThrowOnError extends boolean = false>(options?: Options<GetApiTvChannelsData, ThrowOnError>) => (options?.client ?? client).get<GetApiTvChannelsResponses, unknown, ThrowOnError>({ url: '/api/tv/channels', ...options });

/**
 * Stream current TV game
 *
 * Stream positions and moves of the current [TV game](https://lichess.org/tv) in [ndjson](#description/streaming-with-nd-json).
 * Try it with `curl https://lichess.org/api/tv/feed`.
 *
 */
export const streamCurrentTvGame = <ThrowOnError extends boolean = false>(options?: Options<GetApiTvFeedData, ThrowOnError>) => (options?.client ?? client).get<GetApiTvFeedResponses, unknown, ThrowOnError>({ url: '/api/tv/feed', ...options });

/**
 * Stream current TV game of a TV channel
 *
 * Stream positions and moves of a current [TV channel's game](https://lichess.org/tv/rapid) in [ndjson](#description/streaming-with-nd-json).
 * Try it with `curl https://lichess.org/api/tv/rapid/feed`.
 *
 */
export const streamCurrentTvGameOfATvChannel = <ThrowOnError extends boolean = false>(options: Options<GetApiTvByChannelFeedData, ThrowOnError>) => (options.client ?? client).get<GetApiTvByChannelFeedResponses, unknown, ThrowOnError>({ url: '/api/tv/{channel}/feed', ...options });

/**
 * Get best ongoing games of a TV channel
 *
 * Get a list of ongoing games for a given TV channel. Similar to [lichess.org/games](https://lichess.org/games).
 * Available in PGN or [ndjson](#description/streaming-with-nd-json) format, depending on the request `Accept` header.
 *
 */
export const getBestOngoingGamesOfATvChannel = <ThrowOnError extends boolean = false>(options: Options<GetApiTvByChannelData, ThrowOnError>) => (options.client ?? client).get<GetApiTvByChannelResponses, unknown, ThrowOnError>({ url: '/api/tv/{channel}', ...options });

/**
 * Get current tournaments
 *
 * Get recently active and finished tournaments.
 * This API is used to display the [Lichess tournament schedule](https://lichess.org/tournament).
 *
 */
export const getCurrentTournaments = <ThrowOnError extends boolean = false>(options?: Options<GetApiTournamentData, ThrowOnError>) => (options?.client ?? client).get<GetApiTournamentResponses, unknown, ThrowOnError>({ url: '/api/tournament', ...options });

/**
 * Create a new Arena tournament
 *
 * Create a public or private Arena tournament.
 * This endpoint mirrors the form on <https://lichess.org/tournament/new>.
 * You can create up to 12 public tournaments per day, or 24 private tournaments.
 * A team battle can be created by specifying the `teamBattleByTeam` argument.
 * Additional restrictions:
 * - clockTime + clockIncrement > 0
 * - 15s and 0+1 variant tournaments cannot be rated
 * - Clock time in comparison to tournament length must be reasonable: 3 <= (minutes * 60) / (96 * clockTime + 48 * clockIncrement + 15) <= 150
 *
 */
export const createANewArenaTournament = <ThrowOnError extends boolean = false>(options: Options<PostApiTournamentData, ThrowOnError>) => (options.client ?? client).post<PostApiTournamentResponses, PostApiTournamentErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/tournament',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Get info about an Arena tournament
 *
 * Get detailed info about recently finished, current, or upcoming tournament's duels, player standings, and other info.
 *
 */
export const getInfoAboutAnArenaTournament = <ThrowOnError extends boolean = false>(options: Options<GetApiTournamentByIdData, ThrowOnError>) => (options.client ?? client).get<GetApiTournamentByIdResponses, unknown, ThrowOnError>({ url: '/api/tournament/{id}', ...options });

/**
 * Update an Arena tournament
 *
 * Update an Arena tournament.
 * Be mindful not to make important changes to ongoing tournaments.
 * Can be used to update a team battle.
 * Additional restrictions:
 * - clockTime + clockIncrement > 0
 * - 15s and 0+1 variant tournaments cannot be rated
 * - Clock time in comparison to tournament length must be reasonable: 3 <= (minutes * 60) / (96 * clockTime + 48 * clockIncrement + 15) <= 150
 *
 */
export const updateAnArenaTournament = <ThrowOnError extends boolean = false>(options: Options<PostApiTournamentByIdData, ThrowOnError>) => (options.client ?? client).post<PostApiTournamentByIdResponses, PostApiTournamentByIdErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/tournament/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Join an Arena tournament
 *
 * Join an Arena tournament, possibly with a password and/or a team.
 * Also unpauses if you had previously [paused](#tag/arena-tournaments/POST/api/tournament/{id}/withdraw) the tournament.
 *
 */
export const joinAnArenaTournament = <ThrowOnError extends boolean = false>(options: Options<PostApiTournamentByIdJoinData, ThrowOnError>) => (options.client ?? client).post<PostApiTournamentByIdJoinResponses, PostApiTournamentByIdJoinErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/tournament/{id}/join',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Pause or leave an Arena tournament
 *
 * Leave a future Arena tournament, or take a break on an ongoing Arena tournament.
 * It's possible to join again later. Points and streaks are preserved.
 *
 */
export const pauseOrLeaveAnArenaTournament = <ThrowOnError extends boolean = false>(options: Options<PostApiTournamentByIdWithdrawData, ThrowOnError>) => (options.client ?? client).post<PostApiTournamentByIdWithdrawResponses, PostApiTournamentByIdWithdrawErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/tournament/{id}/withdraw',
    ...options
});

/**
 * Terminate an Arena tournament
 *
 * Terminate an Arena tournament
 *
 */
export const terminateAnArenaTournament = <ThrowOnError extends boolean = false>(options: Options<PostApiTournamentByIdTerminateData, ThrowOnError>) => (options.client ?? client).post<PostApiTournamentByIdTerminateResponses, PostApiTournamentByIdTerminateErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/tournament/{id}/terminate',
    ...options
});

/**
 * Update a team battle
 *
 * Set the teams and number of leaders of a team battle.
 * To update the other attributes of a team battle, use the [tournament update endpoint](#tag/arena-tournaments/POST/api/tournament/{id}).
 *
 */
export const updateATeamBattle = <ThrowOnError extends boolean = false>(options: Options<PostApiTournamentTeamBattleByIdData, ThrowOnError>) => (options.client ?? client).post<PostApiTournamentTeamBattleByIdResponses, PostApiTournamentTeamBattleByIdErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/tournament/team-battle/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Export games of an Arena tournament
 *
 * Download games of a tournament in PGN or [ndjson](#description/streaming-with-nd-json) format.
 * Games are sorted by reverse chronological order (most recent first).
 * The game stream is throttled, depending on who is making the request:
 * - Anonymous request: 20 games per second
 * - [OAuth2 authenticated](#description/authentication) request: 30 games per second
 *
 */
export const exportGamesOfAnArenaTournament = <ThrowOnError extends boolean = false>(options: Options<GetApiTournamentByIdGamesData, ThrowOnError>) => (options.client ?? client).get<GetApiTournamentByIdGamesResponses, unknown, ThrowOnError>({ url: '/api/tournament/{id}/games', ...options });

/**
 * Get results of an Arena tournament
 *
 * Players of an Arena tournament, with their score and performance, sorted by rank (best first).
 * **Players are streamed as [ndjson](#description/streaming-with-nd-json)**, i.e. one JSON object per line.
 * If called on an ongoing tournament, results can be inconsistent
 * due to ranking changes while the players are being streamed.
 * Use on finished tournaments for guaranteed consistency.
 *
 */
export const getResultsOfAnArenaTournament = <ThrowOnError extends boolean = false>(options: Options<GetApiTournamentByIdResultsData, ThrowOnError>) => (options.client ?? client).get<GetApiTournamentByIdResultsResponses, unknown, ThrowOnError>({ url: '/api/tournament/{id}/results', ...options });

/**
 * Get team standing of a team battle
 *
 * Teams of a team battle tournament, with top players, sorted by rank (best first).
 *
 */
export const getTeamStandingOfATeamBattle = <ThrowOnError extends boolean = false>(options: Options<GetApiTournamentByIdTeamsData, ThrowOnError>) => (options.client ?? client).get<GetApiTournamentByIdTeamsResponses, unknown, ThrowOnError>({ url: '/api/tournament/{id}/teams', ...options });

/**
 * Get tournaments created by a user
 *
 * Get all tournaments created by a given user.
 * Tournaments are sorted by reverse chronological order of start date (last starting first).
 * Tournaments are streamed as [ndjson](#description/streaming-with-nd-json).
 * The stream is throttled, depending on who is making the request:
 * - Anonymous request: 20 tournaments per second
 * - [OAuth2 authenticated](#description/authentication) request: 30 tournaments per second
 * - Authenticated, downloading your own tournaments: 50 tournaments per second
 *
 */
export const getTournamentsCreatedByAuser = <ThrowOnError extends boolean = false>(options: Options<GetApiUserByUsernameTournamentCreatedData, ThrowOnError>) => (options.client ?? client).get<GetApiUserByUsernameTournamentCreatedResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/user/{username}/tournament/created',
    ...options
});

/**
 * Get tournaments played by a user
 *
 * Get all tournaments played by a given user.
 * Tournaments are sorted by reverse chronological order of start date (last played first).
 * Tournaments are streamed as [ndjson](#description/streaming-with-nd-json).
 * The stream is throttled, depending on who is making the request:
 * - Anonymous request: 20 tournaments per second
 * - [OAuth2 authenticated](#description/authentication) request: 30 tournaments per second
 * - Authenticated, downloading your own tournaments: 50 tournaments per second
 *
 */
export const getTournamentsPlayedByAuser = <ThrowOnError extends boolean = false>(options: Options<GetApiUserByUsernameTournamentPlayedData, ThrowOnError>) => (options.client ?? client).get<GetApiUserByUsernameTournamentPlayedResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/user/{username}/tournament/played',
    ...options
});

/**
 * Create a new Swiss tournament
 *
 * Create a Swiss tournament for your team.
 * This endpoint mirrors the Swiss tournament form from your team pagee.
 * You can create up to 12 tournaments per day.
 * Additional restrictions:
 * - clock.limit + clock.increment > 0
 * - 15s and 0+1 variant tournaments cannot be rated
 *
 */
export const createANewSwissTournament = <ThrowOnError extends boolean = false>(options: Options<PostApiSwissNewByTeamIdData, ThrowOnError>) => (options.client ?? client).post<PostApiSwissNewByTeamIdResponses, PostApiSwissNewByTeamIdErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/swiss/new/{teamId}',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Get info about a Swiss tournament
 *
 * Get detailed info about a Swiss tournament.
 *
 */
export const getInfoAboutASwissTournament = <ThrowOnError extends boolean = false>(options: Options<GetApiSwissByIdData, ThrowOnError>) => (options.client ?? client).get<GetApiSwissByIdResponses, unknown, ThrowOnError>({ url: '/api/swiss/{id}', ...options });

/**
 * Update a Swiss tournament
 *
 * Update a Swiss tournament.
 * Be mindful not to make important changes to ongoing tournaments.
 * Additional restrictions:
 * - clock.limit + clock.increment > 0
 * - 15s and 0+1 variant tournaments cannot be rated
 *
 */
export const updateASwissTournament = <ThrowOnError extends boolean = false>(options: Options<PostApiSwissByIdEditData, ThrowOnError>) => (options.client ?? client).post<PostApiSwissByIdEditResponses, PostApiSwissByIdEditErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/swiss/{id}/edit',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Manually schedule the next round
 *
 * Manually schedule the next round date and time of a Swiss tournament.
 * This sets the `roundInterval` field to `99999999`, i.e. manual scheduling.
 * All further rounds will need to be manually scheduled, unless the `roundInterval` field is changed back to automatic scheduling.
 *
 */
export const manuallyScheduleTheNextRound = <ThrowOnError extends boolean = false>(options: Options<PostApiSwissByIdScheduleNextRoundData, ThrowOnError>) => (options.client ?? client).post<PostApiSwissByIdScheduleNextRoundResponses, PostApiSwissByIdScheduleNextRoundErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/swiss/{id}/schedule-next-round',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Join a Swiss tournament
 *
 * Join a Swiss tournament, possibly with a password.
 *
 */
export const joinASwissTournament = <ThrowOnError extends boolean = false>(options: Options<PostApiSwissByIdJoinData, ThrowOnError>) => (options.client ?? client).post<PostApiSwissByIdJoinResponses, PostApiSwissByIdJoinErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/swiss/{id}/join',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Pause or leave a swiss tournament
 *
 * Leave a future Swiss tournament, or take a break on an ongoing Swiss tournament.
 * It's possible to join again later. Points are preserved.
 *
 */
export const pauseOrLeaveASwissTournament = <ThrowOnError extends boolean = false>(options: Options<PostApiSwissByIdWithdrawData, ThrowOnError>) => (options.client ?? client).post<PostApiSwissByIdWithdrawResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/swiss/{id}/withdraw',
    ...options
});

/**
 * Terminate a Swiss tournament
 *
 * Terminate a Swiss tournament
 *
 */
export const terminateASwissTournament = <ThrowOnError extends boolean = false>(options: Options<PostApiSwissByIdTerminateData, ThrowOnError>) => (options.client ?? client).post<PostApiSwissByIdTerminateResponses, PostApiSwissByIdTerminateErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/swiss/{id}/terminate',
    ...options
});

/**
 * Export TRF of a Swiss tournament
 *
 * Download a tournament in the Tournament Report File format, the FIDE standard.
 * Documentation: <https://www.fide.com/FIDE/handbook/C04Annex2_TRF16.pdf>
 * Example: <https://lichess.org/swiss/j8rtJ5GL.trf>
 *
 */
export const exportTrfOfASwissTournament = <ThrowOnError extends boolean = false>(options: Options<GetSwissByIdTrfData, ThrowOnError>) => (options.client ?? client).get<GetSwissByIdTrfResponses, unknown, ThrowOnError>({ url: '/swiss/{id}.trf', ...options });

/**
 * Export games of a Swiss tournament
 *
 * Download games of a swiss tournament in PGN or [ndjson](#description/streaming-with-nd-json) format.
 * Games are sorted by chronological order.
 * The game stream is throttled, depending on who is making the request:
 * - Anonymous request: 20 games per second
 * - [OAuth2 authenticated](#description/authentication) request: 30 games per second
 *
 */
export const exportGamesOfASwissTournament = <ThrowOnError extends boolean = false>(options: Options<GetApiSwissByIdGamesData, ThrowOnError>) => (options.client ?? client).get<GetApiSwissByIdGamesResponses, unknown, ThrowOnError>({ url: '/api/swiss/{id}/games', ...options });

/**
 * Get results of a swiss tournament
 *
 * Players of a swiss tournament, with their score and performance, sorted by rank (best first).
 * Players are streamed as [ndjson](#description/streaming-with-nd-json).
 * If called on an ongoing tournament, results can be inconsistent
 * due to ranking changes while the players are being streamed.
 * Use on finished tournaments for guaranteed consistency.
 *
 */
export const getResultsOfASwissTournament = <ThrowOnError extends boolean = false>(options: Options<GetApiSwissByIdResultsData, ThrowOnError>) => (options.client ?? client).get<GetApiSwissByIdResultsResponses, unknown, ThrowOnError>({ url: '/api/swiss/{id}/results', ...options });

/**
 * Get team swiss tournaments
 *
 * Get all swiss tournaments of a team.
 * Tournaments are sorted by reverse chronological order of start date (last starting first).
 * Tournaments are streamed as [ndjson](#description/streaming-with-nd-json).
 *
 */
export const getTeamSwissTournaments = <ThrowOnError extends boolean = false>(options: Options<GetApiTeamByTeamIdSwissData, ThrowOnError>) => (options.client ?? client).get<GetApiTeamByTeamIdSwissResponses, unknown, ThrowOnError>({ url: '/api/team/{teamId}/swiss', ...options });

/**
 * Export one study chapter
 *
 * Download one study chapter in PGN format.
 * If authenticated, then all public, unlisted, and private study chapters are read.
 * If not, only public (non-unlisted) study chapters are read.
 *
 */
export const exportOneStudyChapter = <ThrowOnError extends boolean = false>(options: Options<GetApiStudyByStudyIdByChapterIdPgnData, ThrowOnError>) => (options.client ?? client).get<GetApiStudyByStudyIdByChapterIdPgnResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/study/{studyId}/{chapterId}.pgn',
    ...options
});

/**
 * Export all chapters
 *
 * Download all chapters of a study in PGN format.
 * If authenticated, then all public, unlisted, and private study chapters are read.
 * If not, only public (non-unlisted) study chapters are read.
 *
 */
export const exportAllChapters = <ThrowOnError extends boolean = false>(options: Options<GetApiStudyByStudyIdPgnData, ThrowOnError>) => (options.client ?? client).get<GetApiStudyByStudyIdPgnResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/study/{studyId}.pgn',
    ...options
});

/**
 * Study metadata
 *
 * Only get the study headers, including `Last-Modified`.
 *
 */
export const studyMetadata = <ThrowOnError extends boolean = false>(options: Options<HeadApiStudyByStudyIdPgnData, ThrowOnError>) => (options.client ?? client).head<HeadApiStudyByStudyIdPgnResponses, unknown, ThrowOnError>({ url: '/api/study/{studyId}.pgn', ...options });

/**
 * Create a new Study
 *
 * Create a [study](https://lichess.org/study), and a new empty chapter within it.
 * You can make up to 30 new studies per day.
 *
 */
export const createANewStudy = <ThrowOnError extends boolean = false>(options: Options<PostApiStudyData, ThrowOnError>) => (options.client ?? client).post<PostApiStudyResponses, PostApiStudyErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/study',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Import PGN into a study
 *
 * Imports arbitrary PGN into an existing [study](https://lichess.org/study). Creates a new chapter in the study.
 * If the PGN contains multiple games (separated by 2 or more newlines)
 * then multiple chapters will be created within the study.
 * Note that a study can contain at most 64 chapters.
 *
 */
export const importPgnIntoAStudy = <ThrowOnError extends boolean = false>(options: Options<PostApiStudyByStudyIdImportPgnData, ThrowOnError>) => (options.client ?? client).post<PostApiStudyByStudyIdImportPgnResponses, PostApiStudyByStudyIdImportPgnErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/study/{studyId}/import-pgn',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Update PGN tags of a study chapter
 *
 * Add, update and delete the PGN tags of a study.
 * By providing a list of PGN tags in the usual PGN format, you can:
 * - Add new tags if the chapter doesn't have them yet
 * - Update existing chapter tags
 * - Delete existing chapter tags, by providing a tag with an empty value.
 *
 * The chapter keeps the tags that you don't provide.
 *
 */
export const updatePgnTagsOfAStudyChapter = <ThrowOnError extends boolean = false>(options: Options<PostApiStudyByStudyIdByChapterIdTagsData, ThrowOnError>) => (options.client ?? client).post<PostApiStudyByStudyIdByChapterIdTagsResponses, PostApiStudyByStudyIdByChapterIdTagsErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/study/{studyId}/{chapterId}/tags',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Update the moves of a study chapter
 *
 * Replaces the moves tree of a study chapter.
 * No tags will be modified.
 *
 */
export const updateTheMovesOfAStudyChapter = <ThrowOnError extends boolean = false>(options: Options<PostApiStudyByStudyIdByChapterIdMovesData, ThrowOnError>) => (options.client ?? client).post<PostApiStudyByStudyIdByChapterIdMovesResponses, PostApiStudyByStudyIdByChapterIdMovesErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/study/{studyId}/{chapterId}/moves',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Export all studies of a user
 *
 * Download all chapters of all studies of a user in PGN format.
 * If authenticated, then all public, unlisted, and private studies are included.
 * If not, only public (non-unlisted) studies are included.
 *
 */
export const exportAllStudiesOfAuser = <ThrowOnError extends boolean = false>(options: Options<GetApiStudyByByUsernameExportPgnData, ThrowOnError>) => (options.client ?? client).get<GetApiStudyByByUsernameExportPgnResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/study/by/{username}/export.pgn',
    ...options
});

/**
 * List studies of a user
 *
 * Get metadata (name and dates) of all studies of a user.
 * If authenticated, then all public, unlisted, and private studies are included.
 * If not, only public (non-unlisted) studies are included.
 * Studies are streamed as [ndjson](#description/streaming-with-nd-json).
 *
 */
export const listStudiesOfAuser = <ThrowOnError extends boolean = false>(options: Options<GetApiStudyByByUsernameData, ThrowOnError>) => (options.client ?? client).get<GetApiStudyByByUsernameResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/study/by/{username}',
    ...options
});

/**
 * Delete a study chapter
 *
 * Delete a chapter of a study you own. This is definitive.
 * A study must have at least one chapter; so if you delete the last chapter,
 * an empty one will be automatically created to replace it.
 *
 */
export const deleteAStudyChapter = <ThrowOnError extends boolean = false>(options: Options<DeleteApiStudyByStudyIdByChapterIdData, ThrowOnError>) => (options.client ?? client).delete<DeleteApiStudyByStudyIdByChapterIdResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/study/{studyId}/{chapterId}',
    ...options
});

/**
 * Get official broadcasts
 *
 * Returns ongoing official broadcasts sorted by tier.
 * After that, returns finished broadcasts sorted by most recent sync time.
 * Broadcasts are streamed as [ndjson](#description/streaming-with-nd-json).
 *
 */
export const getOfficialBroadcasts = <ThrowOnError extends boolean = false>(options?: Options<GetApiBroadcastData, ThrowOnError>) => (options?.client ?? client).get<GetApiBroadcastResponses, unknown, ThrowOnError>({ url: '/api/broadcast', ...options });

/**
 * Get paginated top broadcast previews
 *
 * The same data, in the same order, as can be seen on [https://lichess.org/broadcast](/broadcast).
 *
 */
export const getPaginatedTopBroadcastPreviews = <ThrowOnError extends boolean = false>(options?: Options<GetApiBroadcastTopData, ThrowOnError>) => (options?.client ?? client).get<GetApiBroadcastTopResponses, unknown, ThrowOnError>({ url: '/api/broadcast/top', ...options });

/**
 * Get broadcasts created by a user
 *
 * Get all incoming, ongoing, and finished official broadcasts.
 * The broadcasts are sorted by created date, most recent first.
 *
 * If you are authenticated as the user whose broadcasts you are requesting, you will also see your private and unlisted broadcasts.
 *
 */
export const getBroadcastsCreatedByAuser = <ThrowOnError extends boolean = false>(options: Options<GetApiBroadcastByByUsernameData, ThrowOnError>) => (options.client ?? client).get<GetApiBroadcastByByUsernameResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/broadcast/by/{username}',
    ...options
});

/**
 * Search broadcasts
 *
 * Search across recent official broadcasts.
 *
 */
export const searchBroadcasts = <ThrowOnError extends boolean = false>(options?: Options<GetApiBroadcastSearchData, ThrowOnError>) => (options?.client ?? client).get<GetApiBroadcastSearchResponses, unknown, ThrowOnError>({ url: '/api/broadcast/search', ...options });

/**
 * Create a broadcast tournament
 *
 * Create a new broadcast tournament to relay external games.
 * This endpoint accepts the same form data as the [web form](https://lichess.org/broadcast/new).
 *
 */
export const createABroadcastTournament = <ThrowOnError extends boolean = false>(options: Options<PostBroadcastNewData, ThrowOnError>) => (options.client ?? client).post<PostBroadcastNewResponses, PostBroadcastNewErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/broadcast/new',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Get a broadcast tournament
 *
 * Get information about a broadcast tournament.
 *
 */
export const getABroadcastTournament = <ThrowOnError extends boolean = false>(options: Options<GetApiBroadcastByBroadcastTournamentIdData, ThrowOnError>) => (options.client ?? client).get<GetApiBroadcastByBroadcastTournamentIdResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/broadcast/{broadcastTournamentId}',
    ...options
});

/**
 * Get players of a broadcast
 *
 * Get the list of players of a broadcast tournament, if available.
 *
 */
export const getPlayersOfABroadcast = <ThrowOnError extends boolean = false>(options: Options<GetBroadcastByBroadcastTournamentIdPlayersData, ThrowOnError>) => (options.client ?? client).get<GetBroadcastByBroadcastTournamentIdPlayersResponses, unknown, ThrowOnError>({ url: '/broadcast/{broadcastTournamentId}/players', ...options });

/**
 * Get a player of a broadcast
 *
 * Get the details of a specific player and their games from a broadcast tournament.
 *
 */
export const getAPlayerOfABroadcast = <ThrowOnError extends boolean = false>(options: Options<GetBroadcastByBroadcastTournamentIdPlayersByPlayerIdData, ThrowOnError>) => (options.client ?? client).get<GetBroadcastByBroadcastTournamentIdPlayersByPlayerIdResponses, GetBroadcastByBroadcastTournamentIdPlayersByPlayerIdErrors, ThrowOnError>({ url: '/broadcast/{broadcastTournamentId}/players/{playerId}', ...options });

/**
 * Get the team leaderboard of a broadcast
 *
 * Get the team leaderboard of a broadcast tournament, if available.
 *
 */
export const getTheTeamLeaderboardOfABroadcast = <ThrowOnError extends boolean = false>(options: Options<GetBroadcastByBroadcastTournamentIdTeamsStandingsData, ThrowOnError>) => (options.client ?? client).get<GetBroadcastByBroadcastTournamentIdTeamsStandingsResponses, GetBroadcastByBroadcastTournamentIdTeamsStandingsErrors, ThrowOnError>({ url: '/broadcast/{broadcastTournamentId}/teams/standings', ...options });

/**
 * Update your broadcast tournament
 *
 * Update information about a broadcast tournament that you created.
 * This endpoint accepts the same form data as the web form.
 * All fields must be populated with data. Missing fields will override the broadcast with empty data.
 *
 */
export const updateYourBroadcastTournament = <ThrowOnError extends boolean = false>(options: Options<PostBroadcastByBroadcastTournamentIdEditData, ThrowOnError>) => (options.client ?? client).post<PostBroadcastByBroadcastTournamentIdEditResponses, PostBroadcastByBroadcastTournamentIdEditErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/broadcast/{broadcastTournamentId}/edit',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Create a broadcast round
 *
 * Create a new broadcast round to relay external games.
 * This endpoint accepts the same form data as the web form.
 *
 * Choose one between `syncUrl`, `syncUrls`, `syncIds` and `syncUsers`, if it is missing, the broadcast needs to be fed by [pushing PGN to it](#tag/broadcasts/POST/api/broadcast/round/{broadcastRoundId}/push)
 *
 */
export const createABroadcastRound = <ThrowOnError extends boolean = false>(options: Options<PostBroadcastByBroadcastTournamentIdNewData, ThrowOnError>) => (options.client ?? client).post<PostBroadcastByBroadcastTournamentIdNewResponses, PostBroadcastByBroadcastTournamentIdNewErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/broadcast/{broadcastTournamentId}/new',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Get a broadcast round
 *
 * Get information about a broadcast round.
 *
 */
export const getABroadcastRound = <ThrowOnError extends boolean = false>(options: Options<GetApiBroadcastByBroadcastTournamentSlugByBroadcastRoundSlugByBroadcastRoundIdData, ThrowOnError>) => (options.client ?? client).get<GetApiBroadcastByBroadcastTournamentSlugByBroadcastRoundSlugByBroadcastRoundIdResponses, unknown, ThrowOnError>({ url: '/api/broadcast/{broadcastTournamentSlug}/{broadcastRoundSlug}/{broadcastRoundId}', ...options });

/**
 * Update a broadcast round
 *
 * Update information about a broadcast round.
 * This endpoint accepts the same form data as the web form.
 * All fields must be populated with data. Missing fields will override the broadcast with empty data.
 * For instance, if you omit `startDate`, then any pre-existing start date will be removed.
 *
 */
export const updateABroadcastRound = <ThrowOnError extends boolean = false>(options: Options<PostBroadcastRoundByBroadcastRoundIdEditData, ThrowOnError>) => (options.client ?? client).post<PostBroadcastRoundByBroadcastRoundIdEditResponses, PostBroadcastRoundByBroadcastRoundIdEditErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/broadcast/round/{broadcastRoundId}/edit',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Reset a broadcast round
 *
 * Remove any games from the broadcast round and reset it to its initial state.
 *
 */
export const resetABroadcastRound = <ThrowOnError extends boolean = false>(options: Options<PostApiBroadcastRoundByBroadcastRoundIdResetData, ThrowOnError>) => (options.client ?? client).post<PostApiBroadcastRoundByBroadcastRoundIdResetResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/broadcast/round/{broadcastRoundId}/reset',
    ...options
});

/**
 * Push PGN to a broadcast round
 *
 * Update a broadcast with new PGN.
 * Only for broadcasts without a source URL.
 *
 */
export const pushPgnToABroadcastRound = <ThrowOnError extends boolean = false>(options: Options<PostApiBroadcastRoundByBroadcastRoundIdPushData, ThrowOnError>) => (options.client ?? client).post<PostApiBroadcastRoundByBroadcastRoundIdPushResponses, PostApiBroadcastRoundByBroadcastRoundIdPushErrors, ThrowOnError>({
    bodySerializer: null,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/broadcast/round/{broadcastRoundId}/push',
    ...options,
    headers: {
        'Content-Type': 'text/plain',
        ...options.headers
    }
});

/**
 * Stream an ongoing broadcast round as PGN
 *
 * This streaming endpoint first sends all games of a broadcast round in PGN format.
 * Then, it waits for new moves to be played. As soon as it happens, the entire PGN of the game is sent to the stream.
 * The stream will also send PGNs when games are added to the round.
 * This is the best way to get updates about an ongoing round. Streaming means no polling,
 * and no pollings means no latency, and minimum impact on the server.
 *
 */
export const streamAnOngoingBroadcastRoundAsPgn = <ThrowOnError extends boolean = false>(options: Options<GetApiStreamBroadcastRoundByBroadcastRoundIdPgnData, ThrowOnError>) => (options.client ?? client).get<GetApiStreamBroadcastRoundByBroadcastRoundIdPgnResponses, unknown, ThrowOnError>({ url: '/api/stream/broadcast/round/{broadcastRoundId}.pgn', ...options });

/**
 * Export one round as PGN
 *
 * Download all games of a single round of a broadcast tournament in PGN format.
 * You *could* poll this endpoint to get updates about a tournament, but it would be slow,
 * and very inefficient.
 * Instead, consider [streaming the tournament](#tag/broadcasts/GET/api/stream/broadcast/round/{broadcastRoundId}.pgn) to get
 * a new PGN every time a game is updated, in real-time.
 *
 */
export const exportOneRoundAsPgn = <ThrowOnError extends boolean = false>(options: Options<GetApiBroadcastRoundByBroadcastRoundIdPgnData, ThrowOnError>) => (options.client ?? client).get<GetApiBroadcastRoundByBroadcastRoundIdPgnResponses, unknown, ThrowOnError>({ url: '/api/broadcast/round/{broadcastRoundId}.pgn', ...options });

/**
 * Export all rounds as PGN
 *
 * Download all games of all rounds of a broadcast in PGN format.
 * If a `study:read` [OAuth token](#tag/OAuth) is provided,
 * the private rounds where the user is a contributor will be available.
 * You may want to [download only the games of a single round](#tag/broadcasts/GET/api/broadcast/round/{broadcastRoundId}.pgn) instead.
 *
 */
export const exportAllRoundsAsPgn = <ThrowOnError extends boolean = false>(options: Options<GetApiBroadcastByBroadcastTournamentIdPgnData, ThrowOnError>) => (options.client ?? client).get<GetApiBroadcastByBroadcastTournamentIdPgnResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/broadcast/{broadcastTournamentId}.pgn',
    ...options
});

/**
 * Get your broadcast rounds
 *
 * Stream all broadcast rounds you are a member of.
 * Also includes broadcasts rounds you did not create, but were invited to.
 * Also includes broadcasts rounds where you're a non-writing member. See the `writeable` flag in the response.
 * Rounds are ordered by rank, which is roughly chronological, most recent first, slightly pondered with popularity.
 *
 */
export const getYourBroadcastRounds = <ThrowOnError extends boolean = false>(options?: Options<GetApiBroadcastMyRoundsData, ThrowOnError>) => (options?.client ?? client).get<GetApiBroadcastMyRoundsResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/broadcast/my-rounds',
    ...options
});

/**
 * Get a FIDE player
 *
 * Get information about a FIDE player.
 *
 */
export const getAFidePlayer = <ThrowOnError extends boolean = false>(options: Options<GetApiFidePlayerByPlayerIdData, ThrowOnError>) => (options.client ?? client).get<GetApiFidePlayerByPlayerIdResponses, unknown, ThrowOnError>({ url: '/api/fide/player/{playerId}', ...options });

/**
 * Get ratings history of a FIDE player
 *
 * Historical standard, rapid and blitz ratings of a FIDE player
 *
 */
export const getRatingsHistoryOfAFidePlayer = <ThrowOnError extends boolean = false>(options: Options<GetApiFidePlayerByPlayerIdRatingsData, ThrowOnError>) => (options.client ?? client).get<GetApiFidePlayerByPlayerIdRatingsResponses, unknown, ThrowOnError>({ url: '/api/fide/player/{playerId}/ratings', ...options });

/**
 * Search FIDE players
 *
 * List of FIDE players search results for a query.
 *
 */
export const searchFidePlayers = <ThrowOnError extends boolean = false>(options: Options<GetApiFidePlayerData, ThrowOnError>) => (options.client ?? client).get<GetApiFidePlayerResponses, unknown, ThrowOnError>({ url: '/api/fide/player', ...options });

/**
 * Get current simuls
 *
 * Get recently created, started, finished, simuls.
 * Created and finished simul lists are not exhaustives, only those with
 * strong enough host will be listed, the same filter is used to display simuls on https://lichess.org/simul.
 * When [authenticated with OAuth2](#description/authentication), the pending list will be populated with your created, but unstarted simuls.
 *
 */
export const getCurrentSimuls = <ThrowOnError extends boolean = false>(options?: Options<GetApiSimulData, ThrowOnError>) => (options?.client ?? client).get<GetApiSimulResponses, unknown, ThrowOnError>({ url: '/api/simul', ...options });

/**
 * Get a single team
 *
 * Public info about a team. Includes the list of publicly visible leaders.
 */
export const getASingleTeam = <ThrowOnError extends boolean = false>(options: Options<GetApiTeamByTeamIdData, ThrowOnError>) => (options.client ?? client).get<GetApiTeamByTeamIdResponses, unknown, ThrowOnError>({ url: '/api/team/{teamId}', ...options });

/**
 * Get popular teams
 *
 * Paginator of the most popular teams.
 *
 */
export const getPopularTeams = <ThrowOnError extends boolean = false>(options?: Options<GetApiTeamAllData, ThrowOnError>) => (options?.client ?? client).get<GetApiTeamAllResponses, unknown, ThrowOnError>({ url: '/api/team/all', ...options });

/**
 * Teams of a player
 *
 * All the teams a player is a member of.
 * Teams that hide their player list are only included if you also belong to the team.
 *
 */
export const teamsOfAPlayer = <ThrowOnError extends boolean = false>(options: Options<GetApiTeamOfByUsernameData, ThrowOnError>) => (options.client ?? client).get<GetApiTeamOfByUsernameResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/team/of/{username}',
    ...options
});

/**
 * Search teams
 *
 * Paginator of team search results for a keyword.
 *
 */
export const searchTeams = <ThrowOnError extends boolean = false>(options?: Options<GetApiTeamSearchData, ThrowOnError>) => (options?.client ?? client).get<GetApiTeamSearchResponses, unknown, ThrowOnError>({ url: '/api/team/search', ...options });

/**
 * Get members of a team
 *
 * Members are sorted by reverse chronological order of joining the team (most recent first).
 * OAuth is only required if the list of members is private.
 * Up to 5,000 users are streamed as [ndjson](#description/streaming-with-nd-json).
 *
 */
export const getMembersOfATeam = <ThrowOnError extends boolean = false>(options: Options<GetApiTeamByTeamIdUsersData, ThrowOnError>) => (options.client ?? client).get<GetApiTeamByTeamIdUsersResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/team/{teamId}/users',
    ...options
});

/**
 * Get team Arena tournaments
 *
 * Get all Arena tournaments relevant to a team.
 * Tournaments are sorted by reverse chronological order of start date (last starting first).
 * Tournaments are streamed as [ndjson](#description/streaming-with-nd-json).
 *
 */
export const getTeamArenaTournaments = <ThrowOnError extends boolean = false>(options: Options<GetApiTeamByTeamIdArenaData, ThrowOnError>) => (options.client ?? client).get<GetApiTeamByTeamIdArenaResponses, unknown, ThrowOnError>({ url: '/api/team/{teamId}/arena', ...options });

/**
 * Join a team
 *
 * Join a team.
 * If the team requires a password but the `password` field is incorrect,
 * then the call fails with `403 Forbidden`.
 * Similarly, if the team join policy requires a confirmation but the
 * `message` parameter is not given, then the call fails with
 * `403 Forbidden`.
 *
 */
export const joinATeam = <ThrowOnError extends boolean = false>(options: Options<PostTeamByTeamIdJoinData, ThrowOnError>) => (options.client ?? client).post<PostTeamByTeamIdJoinResponses, unknown, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/team/{teamId}/join',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Leave a team
 *
 * Leave a team.
 * - <https://lichess.org/team>
 *
 */
export const leaveATeam = <ThrowOnError extends boolean = false>(options: Options<PostTeamByTeamIdQuitData, ThrowOnError>) => (options.client ?? client).post<PostTeamByTeamIdQuitResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/team/{teamId}/quit',
    ...options
});

/**
 * Get join requests
 *
 * Get pending join requests of your team
 */
export const getJoinRequests = <ThrowOnError extends boolean = false>(options: Options<GetApiTeamByTeamIdRequestsData, ThrowOnError>) => (options.client ?? client).get<GetApiTeamByTeamIdRequestsResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/team/{teamId}/requests',
    ...options
});

/**
 * Accept join request
 *
 * Accept someone's request to join your team
 */
export const acceptJoinRequest = <ThrowOnError extends boolean = false>(options: Options<PostApiTeamByTeamIdRequestByUserIdAcceptData, ThrowOnError>) => (options.client ?? client).post<PostApiTeamByTeamIdRequestByUserIdAcceptResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/team/{teamId}/request/{userId}/accept',
    ...options
});

/**
 * Decline join request
 *
 * Decline someone's request to join your team
 */
export const declineJoinRequest = <ThrowOnError extends boolean = false>(options: Options<PostApiTeamByTeamIdRequestByUserIdDeclineData, ThrowOnError>) => (options.client ?? client).post<PostApiTeamByTeamIdRequestByUserIdDeclineResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/team/{teamId}/request/{userId}/decline',
    ...options
});

/**
 * Kick a user from your team
 *
 * Kick a member out of one of your teams.
 * - <https://lichess.org/team>
 *
 */
export const kickAuserFromYourTeam = <ThrowOnError extends boolean = false>(options: Options<PostApiTeamByTeamIdKickByUserIdData, ThrowOnError>) => (options.client ?? client).post<PostApiTeamByTeamIdKickByUserIdResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/team/{teamId}/kick/{userId}',
    ...options
});

/**
 * Message all members
 *
 * Send a private message to all members of a team.
 * You must be a team leader with the "Messages" permission.
 *
 */
export const messageAllMembers = <ThrowOnError extends boolean = false>(options: Options<PostTeamByTeamIdPmAllData, ThrowOnError>) => (options.client ?? client).post<PostTeamByTeamIdPmAllResponses, PostTeamByTeamIdPmAllErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/team/{teamId}/pm-all',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Get live streamers
 *
 * Get basic info about currently streaming users.
 * This API is very fast and cheap on lichess side.
 * So you can call it quite often (like once every 5 seconds).
 *
 */
export const getLiveStreamers = <ThrowOnError extends boolean = false>(options?: Options<GetApiStreamerLiveData, ThrowOnError>) => (options?.client ?? client).get<GetApiStreamerLiveResponses, unknown, ThrowOnError>({ url: '/api/streamer/live', ...options });

/**
 * Get crosstable
 *
 * Get total number of games, and current score, of any two users.
 * If the `matchup` flag is provided, and the users are currently playing, also gets the current match game number and scores.
 *
 */
export const getCrosstable = <ThrowOnError extends boolean = false>(options: Options<GetApiCrosstableByUser1ByUser2Data, ThrowOnError>) => (options.client ?? client).get<GetApiCrosstableByUser1ByUser2Responses, unknown, ThrowOnError>({ url: '/api/crosstable/{user1}/{user2}', ...options });

/**
 * Autocomplete usernames
 *
 * Provides autocompletion options for an incomplete username.
 *
 */
export const autocompleteUsernames = <ThrowOnError extends boolean = false>(options: Options<GetApiPlayerAutocompleteData, ThrowOnError>) => (options.client ?? client).get<GetApiPlayerAutocompleteResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/player/autocomplete',
    ...options
});

/**
 * Get notes for a user
 *
 * Get the private notes that you have added for a user.
 *
 */
export const getNotesForAuser = <ThrowOnError extends boolean = false>(options: Options<GetApiUserByUsernameNoteData, ThrowOnError>) => (options.client ?? client).get<GetApiUserByUsernameNoteResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/user/{username}/note',
    ...options
});

/**
 * Add a note for a user
 *
 * Add a private note available only to you about this account.
 *
 */
export const addANoteForAuser = <ThrowOnError extends boolean = false>(options: Options<PostApiUserByUsernameNoteData, ThrowOnError>) => (options.client ?? client).post<PostApiUserByUsernameNoteResponses, unknown, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/user/{username}/note',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Get users followed by the logged in user
 *
 * Users are streamed as [ndjson](#description/streaming-with-nd-json).
 *
 */
export const getUsersFollowedByTheLoggedInUser = <ThrowOnError extends boolean = false>(options?: Options<GetApiRelFollowingData, ThrowOnError>) => (options?.client ?? client).get<GetApiRelFollowingResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/rel/following',
    ...options
});

/**
 * Follow a player
 *
 * Follow a player, adding them to your list of Lichess friends.
 *
 */
export const followAPlayer = <ThrowOnError extends boolean = false>(options: Options<PostApiRelFollowByUsernameData, ThrowOnError>) => (options.client ?? client).post<PostApiRelFollowByUsernameResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/rel/follow/{username}',
    ...options
});

/**
 * Unfollow a player
 *
 * Unfollow a player, removing them from your list of Lichess friends.
 *
 */
export const unfollowAPlayer = <ThrowOnError extends boolean = false>(options: Options<PostApiRelUnfollowByUsernameData, ThrowOnError>) => (options.client ?? client).post<PostApiRelUnfollowByUsernameResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/rel/unfollow/{username}',
    ...options
});

/**
 * Block a player
 *
 * Block a player, adding them to your list of blocked Lichess users.
 *
 */
export const blockAPlayer = <ThrowOnError extends boolean = false>(options: Options<PostApiRelBlockByUsernameData, ThrowOnError>) => (options.client ?? client).post<PostApiRelBlockByUsernameResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/rel/block/{username}',
    ...options
});

/**
 * Unblock a player
 *
 * Unblock a player, removing them from your list of blocked Lichess users.
 *
 */
export const unblockAPlayer = <ThrowOnError extends boolean = false>(options: Options<PostApiRelUnblockByUsernameData, ThrowOnError>) => (options.client ?? client).post<PostApiRelUnblockByUsernameResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/rel/unblock/{username}',
    ...options
});

/**
 * Stream incoming events
 *
 * Stream the events reaching a lichess user in real time as [ndjson](#description/streaming-with-nd-json).
 *
 * An empty line is sent every 7 seconds for keep alive purposes.
 *
 * Each non-empty line is a JSON object containing a `type` field. Possible values are:
 * - `gameStart` Start of a game
 * - `gameFinish` Completion of a game
 * - `challenge` A player sends you a challenge or you challenge someone
 * - `challengeCanceled` A player cancels their challenge to you
 * - `challengeDeclined` The opponent declines your challenge
 *
 * When the stream opens, all current challenges and games are sent.
 *
 * Only one global event stream can be active at a time. When the stream opens, the previous one with the same access token is closed.
 *
 */
export const streamIncomingEvents = <ThrowOnError extends boolean = false>(options?: Options<GetApiStreamEventData, ThrowOnError>) => (options?.client ?? client).get<GetApiStreamEventResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/stream/event',
    ...options
});

/**
 * Create a seek
 *
 * Create a public seek, to start a game with a random player.
 *
 * ### Real-time seek
 *
 * Specify the `time` and `increment` clock values.
 * The response is streamed but doesn't contain any information.
 *
 * **Keep the connection open to keep the seek active**.
 *
 * If the client closes the connection, the seek is canceled. This way, if the client terminates, the user won't be paired in a game they wouldn't play.
 * When the seek is accepted, or expires, the server closes the connection.
 *
 * **Make sure to also have an [Event stream](#tag/board/GET/api/board/game/stream/{gameId}) open**, to be notified when a game starts.
 * We recommend opening the [Event stream](#tag/board/GET/api/board/game/stream/{gameId}) first, then the seek stream. This way,
 * you won't miss the game event if the seek is accepted immediately.
 *
 * ### Correspondence seek
 *
 * Specify the `days` per turn value.
 * The response is not streamed, it immediately completes with the seek ID. The seek remains active on the server until it is joined by someone.
 *
 */
export const createASeek = <ThrowOnError extends boolean = false>(options?: Options<PostApiBoardSeekData, ThrowOnError>) => (options?.client ?? client).post<PostApiBoardSeekResponses, PostApiBoardSeekErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/board/seek',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options?.headers
    }
});

/**
 * Stream Board game state
 *
 * Stream the state of a game being played with the Board API, as [ndjson](#description/streaming-with-nd-json).
 *
 * Use this endpoint to get updates about the game in real-time, with a single request.
 *
 * Each line is a JSON object containing a `type` field. Possible values are:
 * - `gameFull` Full game data. All values are immutable, except for the `state` field.
 * - `gameState` Current state of the game. Immutable values not included. Sent when a move is played, a draw is offered, or when the game ends.
 * - `chatLine` Chat message sent by a user in the `room` "player" or "spectator".
 * - `opponentGone` Whether the opponent has left the game, and how long before you can claim a win or draw.
 *
 * The first line is always of type `gameFull`.
 *
 * The server closes the stream when the game ends, or if the game has already ended.
 *
 */
export const streamBoardGameState = <ThrowOnError extends boolean = false>(options: Options<GetApiBoardGameStreamByGameIdData, ThrowOnError>) => (options.client ?? client).get<GetApiBoardGameStreamByGameIdResponses, GetApiBoardGameStreamByGameIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/board/game/stream/{gameId}',
    ...options
});

/**
 * Make a Board move
 *
 * Make a move in a game being played with the Board API.
 * The move can also contain a draw offer/agreement.
 *
 */
export const makeABoardMove = <ThrowOnError extends boolean = false>(options: Options<PostApiBoardGameByGameIdMoveByMoveData, ThrowOnError>) => (options.client ?? client).post<PostApiBoardGameByGameIdMoveByMoveResponses, PostApiBoardGameByGameIdMoveByMoveErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/board/game/{gameId}/move/{move}',
    ...options
});

/**
 * Fetch the player chat
 *
 * Get the messages posted in the private game chat, i.e. the chat between the 2 players of the game.
 *
 * Games can also have a public spectator chat.
 *
 */
export const fetchThePlayerChat = <ThrowOnError extends boolean = false>(options: Options<GetApiBoardGameByGameIdChatData, ThrowOnError>) => (options.client ?? client).get<GetApiBoardGameByGameIdChatResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/board/game/{gameId}/chat',
    ...options
});

/**
 * Write in the chat
 *
 * Post a message to the player or spectator chat, in a game being played with the Board API.
 *
 */
export const writeInTheChat = <ThrowOnError extends boolean = false>(options: Options<PostApiBoardGameByGameIdChatData, ThrowOnError>) => (options.client ?? client).post<PostApiBoardGameByGameIdChatResponses, PostApiBoardGameByGameIdChatErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/board/game/{gameId}/chat',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Abort a game
 *
 * Abort a game being played with the Board API.
 *
 */
export const abortAGame = <ThrowOnError extends boolean = false>(options: Options<PostApiBoardGameByGameIdAbortData, ThrowOnError>) => (options.client ?? client).post<PostApiBoardGameByGameIdAbortResponses, PostApiBoardGameByGameIdAbortErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/board/game/{gameId}/abort',
    ...options
});

/**
 * Resign a game
 *
 * Resign a game being played with the Board API.
 *
 */
export const resignAGame = <ThrowOnError extends boolean = false>(options: Options<PostApiBoardGameByGameIdResignData, ThrowOnError>) => (options.client ?? client).post<PostApiBoardGameByGameIdResignResponses, PostApiBoardGameByGameIdResignErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/board/game/{gameId}/resign',
    ...options
});

/**
 * Handle draw offers
 *
 * Create/accept/decline draw offers.
 * - `yes`: Offer a draw, or accept the opponent's draw offer.
 * - `no`: Decline a draw offer from the opponent.
 *
 */
export const handleDrawOffers = <ThrowOnError extends boolean = false>(options: Options<PostApiBoardGameByGameIdDrawByAcceptData, ThrowOnError>) => (options.client ?? client).post<PostApiBoardGameByGameIdDrawByAcceptResponses, PostApiBoardGameByGameIdDrawByAcceptErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/board/game/{gameId}/draw/{accept}',
    ...options
});

/**
 * Handle takeback offers
 *
 * Create/accept/decline takebacks.
 * - `yes`: Propose a takeback, or accept the opponent's takeback offer.
 * - `no`: Decline a takeback offer from the opponent.
 *
 */
export const handleTakebackOffers = <ThrowOnError extends boolean = false>(options: Options<PostApiBoardGameByGameIdTakebackByAcceptData, ThrowOnError>) => (options.client ?? client).post<PostApiBoardGameByGameIdTakebackByAcceptResponses, PostApiBoardGameByGameIdTakebackByAcceptErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/board/game/{gameId}/takeback/{accept}',
    ...options
});

/**
 * Claim victory of a game
 *
 * Claim victory when the opponent has left the game for a while.
 *
 */
export const claimVictoryOfAGame = <ThrowOnError extends boolean = false>(options: Options<PostApiBoardGameByGameIdClaimVictoryData, ThrowOnError>) => (options.client ?? client).post<PostApiBoardGameByGameIdClaimVictoryResponses, PostApiBoardGameByGameIdClaimVictoryErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/board/game/{gameId}/claim-victory',
    ...options
});

/**
 * Claim draw of a game
 *
 * Claim draw when the opponent has left the game for a while.
 *
 */
export const claimDrawOfAGame = <ThrowOnError extends boolean = false>(options: Options<PostApiBoardGameByGameIdClaimDrawData, ThrowOnError>) => (options.client ?? client).post<PostApiBoardGameByGameIdClaimDrawResponses, PostApiBoardGameByGameIdClaimDrawErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/board/game/{gameId}/claim-draw',
    ...options
});

/**
 * Berserk a tournament game
 *
 * Go berserk on an arena tournament game. Halves the clock time, grants an extra point upon winning.
 * Only available in arena tournaments that allow berserk, and before each player has made a move.
 *
 */
export const berserkATournamentGame = <ThrowOnError extends boolean = false>(options: Options<PostApiBoardGameByGameIdBerserkData, ThrowOnError>) => (options.client ?? client).post<PostApiBoardGameByGameIdBerserkResponses, PostApiBoardGameByGameIdBerserkErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/board/game/{gameId}/berserk',
    ...options
});

/**
 * Get online bots
 *
 * Stream the [online bot users](https://lichess.org/player/bots), as [ndjson](#description/streaming-with-nd-json). Throttled to 50 bot users per second.
 */
export const getOnlineBots = <ThrowOnError extends boolean = false>(options?: Options<GetApiBotOnlineData, ThrowOnError>) => (options?.client ?? client).get<GetApiBotOnlineResponses, unknown, ThrowOnError>({ url: '/api/bot/online', ...options });

/**
 * Upgrade to Bot account
 *
 * Upgrade a lichess player account into a Bot account. Only Bot accounts can use the Bot API.
 * The account **cannot have played any game** before becoming a Bot account. The upgrade is **irreversible**. The account will only be able to play as a Bot.
 * To upgrade an account to Bot, use the [official lichess-bot client](https://github.com/lichess-bot-devs/lichess-bot), or follow these steps:
 * - Create an [API access token](https://lichess.org/account/oauth/token/create?scopes[]=bot:play) with "Play bot moves" permission.
 * - `curl -d '' https://lichess.org/api/bot/account/upgrade -H "Authorization: Bearer <yourTokenHere>"`
 * To know if an account has already been upgraded, use the [Get my profile API](#tag/account/GET/api/account):
 * the `title` field should be set to `BOT`.
 *
 */
export const upgradeToBotAccount = <ThrowOnError extends boolean = false>(options?: Options<PostApiBotAccountUpgradeData, ThrowOnError>) => (options?.client ?? client).post<PostApiBotAccountUpgradeResponses, PostApiBotAccountUpgradeErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/bot/account/upgrade',
    ...options
});

/**
 * Stream Bot game state
 *
 * Stream the state of a game being played with the Bot API, as [ndjson](#description/streaming-with-nd-json).
 * Use this endpoint to get updates about the game in real-time, with a single request.
 * Each line is a JSON object containing a `type` field. Possible values are:
 * - `gameFull` Full game data. All values are immutable, except for the `state` field.
 * - `gameState` Current state of the game. Immutable values not included.
 * - `chatLine` Chat message sent by a user (or the bot itself) in the `room` "player" or "spectator".
 * - `opponentGone` Whether the opponent has left the game, and how long before you can claim a win or draw.
 * The first line is always of type `gameFull`.
 *
 */
export const streamBotGameState = <ThrowOnError extends boolean = false>(options: Options<GetApiBotGameStreamByGameIdData, ThrowOnError>) => (options.client ?? client).get<GetApiBotGameStreamByGameIdResponses, GetApiBotGameStreamByGameIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/bot/game/stream/{gameId}',
    ...options
});

/**
 * Make a Bot move
 *
 * Make a move in a game being played with the Bot API.
 * The move can also contain a draw offer/agreement.
 *
 */
export const makeABotMove = <ThrowOnError extends boolean = false>(options: Options<PostApiBotGameByGameIdMoveByMoveData, ThrowOnError>) => (options.client ?? client).post<PostApiBotGameByGameIdMoveByMoveResponses, PostApiBotGameByGameIdMoveByMoveErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/bot/game/{gameId}/move/{move}',
    ...options
});

/**
 * Fetch the game chat
 *
 * Get the messages posted in the game chat
 *
 */
export const fetchTheGameChat = <ThrowOnError extends boolean = false>(options: Options<GetApiBotGameByGameIdChatData, ThrowOnError>) => (options.client ?? client).get<GetApiBotGameByGameIdChatResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/bot/game/{gameId}/chat',
    ...options
});

/**
 * Write in the chat
 *
 * Post a message to the player or spectator chat, in a game being played with the Bot API.
 *
 */
export const writeInTheChat2 = <ThrowOnError extends boolean = false>(options: Options<PostApiBotGameByGameIdChatData, ThrowOnError>) => (options.client ?? client).post<PostApiBotGameByGameIdChatResponses, PostApiBotGameByGameIdChatErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/bot/game/{gameId}/chat',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Abort a game
 *
 * Abort a game being played with the Bot API.
 *
 */
export const abortAGame2 = <ThrowOnError extends boolean = false>(options: Options<PostApiBotGameByGameIdAbortData, ThrowOnError>) => (options.client ?? client).post<PostApiBotGameByGameIdAbortResponses, PostApiBotGameByGameIdAbortErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/bot/game/{gameId}/abort',
    ...options
});

/**
 * Resign a game
 *
 * Resign a game being played with the Bot API.
 *
 */
export const resignAGame2 = <ThrowOnError extends boolean = false>(options: Options<PostApiBotGameByGameIdResignData, ThrowOnError>) => (options.client ?? client).post<PostApiBotGameByGameIdResignResponses, PostApiBotGameByGameIdResignErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/bot/game/{gameId}/resign',
    ...options
});

/**
 * Handle draw offers
 *
 * Create/accept/decline draw offers with the Bot API.
 * - `yes`: Offer a draw, or accept the opponent's draw offer.
 * - `no`: Decline a draw offer from the opponent.
 *
 */
export const handleDrawOffers2 = <ThrowOnError extends boolean = false>(options: Options<PostApiBotGameByGameIdDrawByAcceptData, ThrowOnError>) => (options.client ?? client).post<PostApiBotGameByGameIdDrawByAcceptResponses, PostApiBotGameByGameIdDrawByAcceptErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/bot/game/{gameId}/draw/{accept}',
    ...options
});

/**
 * Handle takeback offers
 *
 * Create/accept/decline takebacks with the Bot API.
 * - `yes`: Propose a takeback, or accept the opponent's takeback offer.
 * - `no`: Decline a takeback offer from the opponent.
 *
 */
export const handleTakebackOffers2 = <ThrowOnError extends boolean = false>(options: Options<PostApiBotGameByGameIdTakebackByAcceptData, ThrowOnError>) => (options.client ?? client).post<PostApiBotGameByGameIdTakebackByAcceptResponses, PostApiBotGameByGameIdTakebackByAcceptErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/bot/game/{gameId}/takeback/{accept}',
    ...options
});

/**
 * Claim victory of a game
 *
 * Claim victory when the opponent has left the game for a while.
 *
 */
export const claimVictoryOfAGame2 = <ThrowOnError extends boolean = false>(options: Options<PostApiBotGameByGameIdClaimVictoryData, ThrowOnError>) => (options.client ?? client).post<PostApiBotGameByGameIdClaimVictoryResponses, PostApiBotGameByGameIdClaimVictoryErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/bot/game/{gameId}/claim-victory',
    ...options
});

/**
 * Claim draw of a game
 *
 * Claim draw when the opponent has left the game for a while.
 *
 */
export const claimDrawOfAGame2 = <ThrowOnError extends boolean = false>(options: Options<PostApiBotGameByGameIdClaimDrawData, ThrowOnError>) => (options.client ?? client).post<PostApiBotGameByGameIdClaimDrawResponses, PostApiBotGameByGameIdClaimDrawErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/bot/game/{gameId}/claim-draw',
    ...options
});

/**
 * List your challenges
 *
 * Get a list of challenges created by or targeted at you.
 *
 */
export const listYourChallenges = <ThrowOnError extends boolean = false>(options?: Options<GetApiChallengeData, ThrowOnError>) => (options?.client ?? client).get<GetApiChallengeResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/challenge',
    ...options
});

/**
 * Create a challenge
 *
 * Challenge someone to play. The targeted player can choose to accept or decline.
 * If the challenge is accepted, you will be notified on the [event stream](#tag/board/GET/api/board/game/stream/{gameId})
 * that a new game has started. The game ID will be the same as the challenge ID.
 * Challenges for realtime games (not correspondence) expire after 20s if not accepted.
 * To prevent that, use the `keepAliveStream` flag described below.
 *
 */
export const createAChallenge = <ThrowOnError extends boolean = false>(options: Options<PostApiChallengeByUsernameData, ThrowOnError>) => (options.client ?? client).post<PostApiChallengeByUsernameResponses, PostApiChallengeByUsernameErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/challenge/{username}',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Show one challenge
 *
 * Get details about a challenge, even if it has been recently accepted, canceled or declined.
 *
 */
export const showOneChallenge = <ThrowOnError extends boolean = false>(options: Options<GetApiChallengeByChallengeIdShowData, ThrowOnError>) => (options.client ?? client).get<GetApiChallengeByChallengeIdShowResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/challenge/{challengeId}/show',
    ...options
});

/**
 * Accept a challenge
 *
 * Accept an incoming challenge.
 * You should receive a `gameStart` event on the [incoming events stream](#tag/board/GET/api/board/game/stream/{gameId}).
 *
 */
export const acceptAChallenge = <ThrowOnError extends boolean = false>(options: Options<PostApiChallengeByChallengeIdAcceptData, ThrowOnError>) => (options.client ?? client).post<PostApiChallengeByChallengeIdAcceptResponses, PostApiChallengeByChallengeIdAcceptErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/challenge/{challengeId}/accept',
    ...options
});

/**
 * Decline a challenge
 *
 * Decline an incoming challenge.
 *
 */
export const declineAChallenge = <ThrowOnError extends boolean = false>(options: Options<PostApiChallengeByChallengeIdDeclineData, ThrowOnError>) => (options.client ?? client).post<PostApiChallengeByChallengeIdDeclineResponses, PostApiChallengeByChallengeIdDeclineErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/challenge/{challengeId}/decline',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Cancel a challenge
 *
 * Cancel a challenge you sent, or aborts the game if the challenge was accepted, but the game was not yet played.
 * Note that the ID of a game is the same as the ID of the challenge that created it.
 * Works for user challenges and open challenges alike.
 *
 */
export const cancelAChallenge = <ThrowOnError extends boolean = false>(options: Options<PostApiChallengeByChallengeIdCancelData, ThrowOnError>) => (options.client ?? client).post<PostApiChallengeByChallengeIdCancelResponses, PostApiChallengeByChallengeIdCancelErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/challenge/{challengeId}/cancel',
    ...options
});

/**
 * Challenge the AI
 *
 * Start a game with Lichess AI.
 * You will be notified on the [event stream](#tag/board/GET/api/board/game/stream/{gameId}) that a new game has started.
 *
 */
export const challengeTheAi = <ThrowOnError extends boolean = false>(options: Options<PostApiChallengeAiData, ThrowOnError>) => (options.client ?? client).post<PostApiChallengeAiResponses, PostApiChallengeAiErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/challenge/ai',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Open-ended challenge
 *
 * Create a challenge that any 2 players can join.
 * Share the URL of the challenge. the first 2 players to click it will be paired for a game.
 * The response body also contains `whiteUrl` and `blackUrl`.
 * You can control which color each player gets by giving them these URLs,
 * instead of the main challenge URL.
 * Open challenges expire after 24h.
 * If the challenge creation is [authenticated with OAuth2](#description/authentication),
 * then you can use the [challenge cancel endpoint](#tag/challenges/POST/api/challenge/{challengeId}/cancel) to cancel it.
 * To directly pair 2 known players, use [this endpoint](#tag/bulk-pairings/GET/api/bulk-pairing) instead.
 *
 */
export const openendedChallenge = <ThrowOnError extends boolean = false>(options?: Options<PostApiChallengeOpenData, ThrowOnError>) => (options?.client ?? client).post<PostApiChallengeOpenResponses, PostApiChallengeOpenErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    url: '/api/challenge/open',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options?.headers
    }
});

/**
 * Start clocks of a game
 *
 * Start the clocks of a game immediately, even if a player has not yet made a move.
 * Requires the OAuth tokens of both players with `challenge:write` scope.
 * If the clocks have already started, the call will have no effect.
 *
 * For AI games with only one player, omit the `token2` parameter.
 *
 */
export const startClocksOfAGame = <ThrowOnError extends boolean = false>(options: Options<PostApiChallengeByGameIdStartClocksData, ThrowOnError>) => (options.client ?? client).post<PostApiChallengeByGameIdStartClocksResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/challenge/{gameId}/start-clocks',
    ...options
});

/**
 * View your bulk pairings
 *
 * Get a list of bulk pairings you created.
 *
 */
export const viewYourBulkPairings = <ThrowOnError extends boolean = false>(options?: Options<GetApiBulkPairingData, ThrowOnError>) => (options?.client ?? client).get<GetApiBulkPairingResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/bulk-pairing',
    ...options
});

/**
 * Create a bulk pairing
 *
 * Schedule many games at once, up to 24h in advance.
 * OAuth tokens are required for all paired players, with the `challenge:write` scope.
 * You can schedule up to 500 games every 10 minutes. [Contact us](mailto:contact@lichess.org) if you need higher limits.
 * If games have a real-time clock, each player must have only one pairing.
 * For correspondence games, players can have multiple pairings within the same bulk.
 *
 * **The entire bulk is rejected if:**
 * - a token is missing
 * - a token is present more than once (except in correspondence)
 * - a token lacks the `challenge:write` scope
 * - a player account is closed
 * - a player is paired more than once (except in correspondence)
 * - a bulk is already scheduled to start at the same time with the same player
 * - you have 20 scheduled bulks
 * - you have 1000 scheduled games
 *
 * Partial bulks are never created. Either it all fails, or it all succeeds.
 * When it fails, it does so with an error message explaining the issue.
 * Failed bulks are not counted in the rate limiting, they are free.
 * Fix the issues, manually or programmatically, then retry to schedule the bulk.
 * A successful bulk creation returns a JSON bulk document. Its ID can be used for further operations.
 *
 */
export const createABulkPairing = <ThrowOnError extends boolean = false>(options: Options<PostApiBulkPairingData, ThrowOnError>) => (options.client ?? client).post<PostApiBulkPairingResponses, PostApiBulkPairingErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/bulk-pairing',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Manually start clocks
 *
 * Immediately start all clocks of the games of a bulk pairing.
 * This overrides the `startClocksAt` value of an existing bulk pairing.
 * If the games have not yet been created (`bulk.pairAt` is in the future), then this does nothing.
 * If the clocks have already started (`bulk.startClocksAt` is in the past), then this does nothing.
 *
 */
export const manuallyStartClocks = <ThrowOnError extends boolean = false>(options: Options<PostApiBulkPairingByIdStartClocksData, ThrowOnError>) => (options.client ?? client).post<PostApiBulkPairingByIdStartClocksResponses, PostApiBulkPairingByIdStartClocksErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/bulk-pairing/{id}/start-clocks',
    ...options
});

/**
 * Cancel a bulk pairing
 *
 * Cancel and delete a bulk pairing that is scheduled in the future.
 * If the games have already been created, then this does nothing.
 * Canceling a bulk pairing does not refund the rate limit cost of that bulk pairing.
 *
 */
export const cancelABulkPairing = <ThrowOnError extends boolean = false>(options: Options<DeleteApiBulkPairingByIdData, ThrowOnError>) => (options.client ?? client).delete<DeleteApiBulkPairingByIdResponses, DeleteApiBulkPairingByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/bulk-pairing/{id}',
    ...options
});

/**
 * Show a bulk pairing
 *
 * Get a single bulk pairing by its ID.
 *
 */
export const showABulkPairing = <ThrowOnError extends boolean = false>(options: Options<GetApiBulkPairingByIdData, ThrowOnError>) => (options.client ?? client).get<GetApiBulkPairingByIdResponses, GetApiBulkPairingByIdErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/bulk-pairing/{id}',
    ...options
});

/**
 * Export games of a bulk pairing
 *
 * Download games of a bulk in PGN or [ndjson](#description/streaming-with-nd-json) format, depending on the request `Accept` header.
 *
 */
export const exportGamesOfABulkPairing = <ThrowOnError extends boolean = false>(options: Options<GetApiBulkPairingByIdGamesData, ThrowOnError>) => (options.client ?? client).get<GetApiBulkPairingByIdGamesResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/bulk-pairing/{id}/games',
    ...options
});

/**
 * Add time to the opponent clock
 *
 * Add seconds to the opponent's clock. Can be used to create games with time odds.
 *
 */
export const addTimeToTheOpponentClock = <ThrowOnError extends boolean = false>(options: Options<PostApiRoundByGameIdAddTimeBySecondsData, ThrowOnError>) => (options.client ?? client).post<PostApiRoundByGameIdAddTimeBySecondsResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/round/{gameId}/add-time/{seconds}',
    ...options
});

/**
 * Admin challenge tokens
 *
 * **This endpoint can only be used by Lichess administrators. It will not work if you do not have the appropriate permissions.** Tournament organizers should instead use [OAuth](#tag/OAuth) to obtain `challenge:write` tokens from users in order to perform bulk pairing.*
 * Create and obtain `challenge:write` tokens for multiple users.
 * If a similar token already exists for a user, it is reused. This endpoint is idempotent.
 *
 */
export const adminChallengeTokens = <ThrowOnError extends boolean = false>(options: Options<PostApiTokenAdminChallengeData, ThrowOnError>) => (options.client ?? client).post<PostApiTokenAdminChallengeResponses, PostApiTokenAdminChallengeErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/token/admin-challenge',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Send a private message
 *
 * Send a private message to another player.
 *
 */
export const sendAPrivateMessage = <ThrowOnError extends boolean = false>(options: Options<PostInboxByUsernameData, ThrowOnError>) => (options.client ?? client).post<PostInboxByUsernameResponses, PostInboxByUsernameErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/inbox/{username}',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Get cloud evaluation of a position.
 *
 * Get the cached evaluation of a position, if available.
 * Opening positions have more chances of being available. There are about 320 million positions in the database.
 * Up to 5 variations may be available. Variants are supported.
 * Use this endpoint to fetch a few positions here and there.
 * If you want to download a lot of positions, [get the full list](https://database.lichess.org/#evals) from our exported database.
 *
 */
export const getCloudEvaluationOfAPosition = <ThrowOnError extends boolean = false>(options: Options<GetApiCloudEvalData, ThrowOnError>) => (options.client ?? client).get<GetApiCloudEvalResponses, GetApiCloudEvalErrors, ThrowOnError>({ url: '/api/cloud-eval', ...options });

/**
 * List external engines
 *
 * Lists all external engines that have been registered for the user,
 * and the credentials required to use them.
 *
 */
export const listExternalEngines = <ThrowOnError extends boolean = false>(options?: Options<GetApiExternalEngineData, ThrowOnError>) => (options?.client ?? client).get<GetApiExternalEngineResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/external-engine',
    ...options
});

/**
 * Create external engine
 *
 * Registers a new external engine for the user. It can then be selected
 * and used on the analysis board.
 * After registering, the provider should start waiting for analyis requests.
 *
 */
export const createExternalEngine = <ThrowOnError extends boolean = false>(options: Options<PostApiExternalEngineData, ThrowOnError>) => (options.client ?? client).post<PostApiExternalEngineResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/external-engine',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete external engine
 *
 * Unregisters an external engine.
 *
 */
export const deleteExternalEngine = <ThrowOnError extends boolean = false>(options: Options<DeleteApiExternalEngineByIdData, ThrowOnError>) => (options.client ?? client).delete<DeleteApiExternalEngineByIdResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/external-engine/{id}',
    ...options
});

/**
 * Get external engine
 *
 * Get properties and credentials of an external engine.
 *
 */
export const getExternalEngine = <ThrowOnError extends boolean = false>(options: Options<GetApiExternalEngineByIdData, ThrowOnError>) => (options.client ?? client).get<GetApiExternalEngineByIdResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/external-engine/{id}',
    ...options
});

/**
 * Update external engine
 *
 * Updates the properties of an external engine.
 *
 */
export const updateExternalEngine = <ThrowOnError extends boolean = false>(options: Options<PutApiExternalEngineByIdData, ThrowOnError>) => (options.client ?? client).put<PutApiExternalEngineByIdResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/external-engine/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Analyse with external engine
 *
 * **Endpoint: `https://engine.lichess.ovh/api/external-engine/{id}/analyse`**
 * Request analysis from an external engine.
 * Response content is streamed as [newline delimited JSON](#description/streaming-with-nd-json).
 * The properties are based on the [UCI specification](https://backscattering.de/chess/uci/#engine).
 * Analysis stops when the client goes away, the requested limit
 * is reached, or the provider goes away.
 *
 */
export const analyseWithExternalEngine = <ThrowOnError extends boolean = false>(options: Options<PostApiExternalEngineByIdAnalyseData, ThrowOnError>) => (options.client ?? client).post<PostApiExternalEngineByIdAnalyseResponses, unknown, ThrowOnError>({
    url: '/api/external-engine/{id}/analyse',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Acquire analysis request
 *
 * **Endpoint: `https://engine.lichess.ovh/api/external-engine/work`**
 * Wait for an analysis requests to any of the external engines that
 * have been registered with the given `secret`.
 * Uses long polling.
 * After acquiring a request, the provider should immediately
 * [start streaming the results](#tag/external-engine/POST/api/external-engine/workid).
 *
 */
export const acquireAnalysisRequest = <ThrowOnError extends boolean = false>(options: Options<PostApiExternalEngineWorkData, ThrowOnError>) => (options.client ?? client).post<PostApiExternalEngineWorkResponses, unknown, ThrowOnError>({
    url: '/api/external-engine/work',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Answer analysis request
 *
 * **Endpoint: `https://engine.lichess.ovh/api/external-engine/work/{id}`**
 * Submit a stream of analysis as [UCI output](https://backscattering.de/chess/uci/#engine-info).
 * * The engine should always be in `UCI_Chess960` mode.
 * * `UCI_AnalyseMode` enabled if available.
 * * It produces `info` with at least:
 * - `depth`
 * - `multipv` (between 1 and 5)
 * - `score`
 * - `nodes`
 * - `time`
 * - `pv`
 * The server may close the connection at any time, indicating that
 * the requester has gone away and analysis should be stopped.
 *
 */
export const answerAnalysisRequest = <ThrowOnError extends boolean = false>(options: Options<PostApiExternalEngineWorkByIdData, ThrowOnError>) => (options.client ?? client).post<PostApiExternalEngineWorkByIdResponses, unknown, ThrowOnError>({
    bodySerializer: null,
    url: '/api/external-engine/work/{id}',
    ...options,
    headers: {
        'Content-Type': 'text/plain',
        ...options.headers
    }
});

/**
 * Request authorization code
 *
 * OAuth2 authorization endpoint.
 * Start the OAuth2 Authorization Code Flow with PKCE by securely
 * generating two random strings unique to each authorization
 * request:
 *
 * * `code_verifier`
 * * `state`
 *
 * Store these in session storage. Make sure not to reveal `code_verifier`
 * to eavesdroppers. Do not show it in URLs, do not abuse `state` to store
 * it, do not send it over insecure connections. However it is fine if
 * the user themselves can extract `code_verifier`, which will always be
 * possible for fully client-side apps.
 * Then send the user to this endpoint. They will be prompted to grant
 * authorization and then be redirected back to the given `redirect_uri`.
 * If the authorization failed, the following query string parameters will
 * be appended to the redirection:
 *
 * * `error`, in particular with value `access_denied` if the user
 * cancelled authorization
 * * `error_description` to aid debugging
 * * `state`, exactly as passed in the `state` parameter
 *
 * If the authorization succeeded, the following query string parameters
 * will be appended to the redirection:
 *
 * * `code`, containing a fresh short-lived authorization code
 * * `state`, exactly as passed in the `state` parameter
 *
 * Next, to defend against cross site request forgery, check that the
 * returned `state` matches the `state` you originally generated.
 *
 * Finally, continue by using the authorization code to
 * [obtain an access token](#tag/oauth/POST/api/token).
 *
 */
export const requestAuthorizationCode = <ThrowOnError extends boolean = false>(options: Options<GetOauthData, ThrowOnError>) => (options.client ?? client).get<GetOauthResponses, unknown, ThrowOnError>({ url: '/oauth', ...options });

/**
 * Revoke access token
 *
 * Revokes the access token sent as Bearer for this request.
 */
export const revokeAccessToken = <ThrowOnError extends boolean = false>(options?: Options<DeleteApiTokenData, ThrowOnError>) => (options?.client ?? client).delete<DeleteApiTokenResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/api/token',
    ...options
});

/**
 * Obtain access token
 *
 * OAuth2 token endpoint. Exchanges an authorization code for an access token.
 *
 */
export const obtainAccessToken = <ThrowOnError extends boolean = false>(options: Options<PostApiTokenData, ThrowOnError>) => (options.client ?? client).post<PostApiTokenResponses, PostApiTokenErrors, ThrowOnError>({
    ...urlSearchParamsBodySerializer,
    url: '/api/token',
    ...options,
    headers: {
        'Content-Type': 'application/x-www-form-urlencoded',
        ...options.headers
    }
});

/**
 * Test multiple OAuth tokens
 *
 * For up to 1000 OAuth tokens,
 * returns their associated user ID and scopes,
 * or `null` if the token is invalid.
 * The method is `POST` so a longer list of tokens can be sent in the request body.
 *
 */
export const testMultipleOauthTokens = <ThrowOnError extends boolean = false>(options: Options<PostApiTokenTestData, ThrowOnError>) => (options.client ?? client).post<PostApiTokenTestResponses, unknown, ThrowOnError>({
    bodySerializer: null,
    url: '/api/token/test',
    ...options,
    headers: {
        'Content-Type': 'text/plain',
        ...options.headers
    }
});

/**
 * Masters database
 *
 * **Endpoint: <https://explorer.lichess.org/masters>**
 *
 * Example: `curl https://explorer.lichess.org/masters?play=d2d4,d7d5,c2c4,c7c6,c4d5`
 *
 */
export const mastersDatabase = <ThrowOnError extends boolean = false>(options?: Options<GetMastersData, ThrowOnError>) => (options?.client ?? client).get<GetMastersResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/masters',
    ...options
});

/**
 * Lichess games
 *
 * **Endpoint: <https://explorer.lichess.org/lichess>**
 *
 * Aggregated rated games from all Lichess players.
 *
 * Example: `curl https://explorer.lichess.org/lichess?variant=standard&speeds=blitz,rapid,classical&ratings=2200,2500&fen=rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR%20w%20KQkq%20-%200%201`
 *
 */
export const lichessGames = <ThrowOnError extends boolean = false>(options?: Options<GetLichessData, ThrowOnError>) => (options?.client ?? client).get<GetLichessResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/lichess',
    ...options
});

/**
 * Player games
 *
 * **Endpoint: <https://explorer.lichess.org/player>**
 *
 * Games of a Lichess player.
 *
 * Responds with a stream of [newline delimited JSON](#description/streaming-with-nd-json). Will start indexing
 * on demand, immediately respond with the current results, and stream
 * more updates until indexing is complete. The stream is throttled
 * and deduplicated. Empty lines may be sent to avoid timeouts.
 *
 * Will index new games at most once per minute, and revisit previously
 * ongoing games at most once every day.
 *
 * Example: `curl https://explorer.lichess.org/player?player=revoof&color=white&play=d2d4,d7d5&recentGames=1`
 *
 */
export const playerGames = <ThrowOnError extends boolean = false>(options: Options<GetPlayerData, ThrowOnError>) => (options.client ?? client).get<GetPlayerResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/player',
    ...options
});

/**
 * OTB master game
 *
 * **Endpoint: `https://explorer.lichess.org/masters/pgn/{gameId}`**
 *
 * Example: `curl https://explorer.lichess.org/masters/pgn/aAbqI4ey`
 *
 */
export const otbMasterGame = <ThrowOnError extends boolean = false>(options: Options<GetMastersPgnByGameIdData, ThrowOnError>) => (options.client ?? client).get<GetMastersPgnByGameIdResponses, unknown, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/masters/pgn/{gameId}',
    ...options
});

/**
 * Tablebase lookup
 *
 * **Endpoint: <https://tablebase.lichess.org>**
 *
 * Example: `curl http://tablebase.lichess.org/standard?fen=4k3/6KP/8/8/8/8/7p/8_w_-_-_0_1`
 *
 */
export const tablebaseLookup = <ThrowOnError extends boolean = false>(options: Options<GetStandardData, ThrowOnError>) => (options.client ?? client).get<GetStandardResponses, unknown, ThrowOnError>({ url: '/standard', ...options });

/**
 * Tablebase lookup for Atomic chess
 *
 * **Endpoint: <https://tablebase.lichess.org>**
 *
 */
export const tablebaseLookupForAtomicChess = <ThrowOnError extends boolean = false>(options: Options<GetAtomicData, ThrowOnError>) => (options.client ?? client).get<GetAtomicResponses, unknown, ThrowOnError>({ url: '/atomic', ...options });

/**
 * Tablebase lookup for Antichess
 *
 * **Endpoint: <https://tablebase.lichess.org>**
 *
 */
export const tablebaseLookupForAntichess = <ThrowOnError extends boolean = false>(options: Options<GetAntichessData, ThrowOnError>) => (options.client ?? client).get<GetAntichessResponses, unknown, ThrowOnError>({ url: '/antichess', ...options });
